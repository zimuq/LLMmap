# D014 — Is γ=0.5's failure a threshold in γ, or an unlucky chain? (fine γ sweep, both algorithms)

```
id:          D014
source:      D013 `## R` R2 and its post-hoc Review (γ=0.5 gave
             GreedyCover no benefit over γ=1.0; single γ point; the
             proxy and trained anomalies are the same nested chain
             evaluated twice); D012's addendum (JointGreedy never run
             between γ=0.25 and γ=1.0); human-approved 2026-09-19, scope
             set to BOTH algorithms
status:      OPEN
gate:        REVIEW          # settles the fine structure of C1 and
             unblocks a held METHOD.md §5.2 wording decision; produces
             numbers a writeup would use — default REVIEW per CLAUDE.md
deps:        D007 (frozen S_energy tensor), D008 (GreedyCover chains;
             half-config stability machinery, R5), D009 (trained
             protocol; γ=1.0/0.1 GreedyCover), D010 (JointGreedy code
             and γ=0.1), D012 (JointGreedy γ ∈ {1.0,0.25,0.1,0.05}),
             D013 (GreedyCover γ ∈ {1.0,0.5,0.25,0.1,0.05}) — all CLOSED;
             every existing grid point is reused, not rerun (I6)
opened:      2026-09-19
closed:      —
downstream:  (1) Decides whether "γ ≤ 0.25" is a measured cutoff or just
             a conservative rule. (2) The human has HELD the METHOD.md
             §5.2 edit ("γ is a clean continuous ablation axis") until
             this lands — this D's verdict decides what that sentence
             should say. (3) Bears on the project's largest unexplained
             question (why a small γ helps selection in general): a real
             threshold in γ would be a clue; a chain artifact would not.
```

---

## D — The question

> **Is the absence of any CVaR benefit at γ=0.5 (D013, `GreedyCover`) a
> property of γ — a threshold or plateau in how γ shapes selection — or
> an accident of that one nested greedy chain? Answer it for
> `GreedyCover` and for `JointGreedy`.**

### What is and is not known

- **Known.** On one chain, `GreedyCover` at γ=0.5 gave no benefit over
  γ=1.0 (if anything slightly worse); γ ∈ {0.25, 0.1, 0.05} all gave the
  full benefit; γ=1.0 is decisively worse.
- **Not known.** Anything about γ between 0.25 and 1.0 except that one
  point. D008's proxy anomaly and D013's trained anomaly are **the same
  chain evaluated two ways** — not an independent replication of "γ=0.5
  is bad." Chains are composition-unstable (D008/R5: half the selected
  queries change when half the build configs do), and one weak early
  pick cascades through a nested greedy chain. TACC's mechanism (CVaR₀.₅
  "commits to neither mean nor tail") predicts a result *between* γ=1
  and γ=0.25, not at or below γ=1 — so the observation is not yet
  explained.

### Why both algorithms — and why that is the sharpest test available

`JointGreedy` has never been run at γ=0.5. More importantly, the two
algorithms select **different** chains for the same γ from `k=2` on
(they coincide only at `k=1`, D010/R2). "Chain luck" is therefore
independent across algorithms. **If the same γ-region fails for both,
luck is a poor explanation; if only one fails, it is chain-specific.**
Cross-algorithm agreement is an independent test of the chain-luck
hypothesis that no amount of extra γ points on a single algorithm can
give.

**Correction, 2026-09-19 (P1 F1) — this overstated the independence.**
Both algorithms are greedy walks over the *same* frozen tensor built from
the *same* corpus and config sample, so noise in that tensor is shared:
a query that is spuriously strong under a given γ tends to be picked by
both (measured `k`=8 overlap is 2–4 of 8). And at `k`=1 the two are the
same algorithm (D010/R2), so any shared first-query behaviour is one
observation, not two. Cross-algorithm agreement (read at `k` ≥ 2 only)
is therefore **evidence against chain luck, not a refutation of it.**
The stronger test is Arm B, which perturbs the shared data itself.

### Pre-registered hypotheses (stated before any new run)

- **H1 — threshold / plateau.** Benefit versus γ is a step. Above some
  cutoff γ\* (somewhere in 0.25–0.5) selection behaves like γ=1 (no
  benefit); below it, the full benefit. *Predicts:* grid points on
  **both** sides of 0.5 close to it (0.6, 0.75 above; 0.45, 0.4 below
  down to γ\*) also show no benefit; a single transition; resampled
  chains at γ=0.5 are consistently poor; the effect appears in both
  algorithms (γ\* may differ).
- **H2 — chain luck.** γ=0.5 is an isolated dip on a smoother curve.
  *Predicts:* neighbours on one or both sides recover benefit in line
  with the trend; resampled chains at γ=0.5 span a wide range that
  overlaps γ=0.25's; the other algorithm is not poor at 0.5.
- **Design-side lean, weakly held (~60/40 for H2):** chains are
  composition-unstable, TACC's mechanism does not predict the observed
  sign, and nothing else supports a discontinuity. Recorded so a wrong
  guess is visible — D012 and D013's pre-registered predictions were
  each at least partly wrong.

---

## What counts as an answer

Primary metric: **mean top-1** (worst-class is too noisy to decide on —
±0.20 CIs, A6 — reported, not used). Reuse D012's machinery: `δ = 0.02`
TOST equivalence, the "CI excludes 0 **and** exceeds the seed range"
rule for a resolved difference, and F4's consistent-sign-across-`k` rule
for any ordering claim. **The exact numeric labelling rule is fixed in
P1 (Call 1), before any training run, and does not move afterwards.**

Each grid γ is labelled, per algorithm, against γ=1.0: **BENEFIT**,
**NO-BENEFIT**, or **UNRESOLVED**. Then, over the ordered grid:

| Outcome (per algorithm) | Pattern |
|---|---|
| **STEP (H1)** | NO-BENEFIT for a contiguous run from γ=1.0 downward, then BENEFIT below a single cutoff, at most one grid point between; resampled-chain distribution at the plateau γ's sits clearly below the low-γ's. |
| **DIP (H2)** | A BENEFIT label at some γ *above* a NO-BENEFIT label (e.g. 0.5 sits between two BENEFIT neighbours), **or** the resampled chains at γ=0.5 substantially overlap γ=0.25's. |
| **INCONCLUSIVE** | Too many UNRESOLVED points to tell STEP from DIP. Report which points, not a forced call. |

**Cross-algorithm reading, stated in advance:** STEP in both → γ is a
real dial with a cutoff (a mechanism clue). DIP in both, or STEP in one
and DIP in the other → the γ=0.5 result was chain-specific, and
"γ ≤ 0.25" remains a conservative rule with no measured cutoff behind it.

**Amendment, 2026-09-19 (Review below) — Arm A alone cannot decide
H1 vs H2; Arm B decides.** P1's S1 showed chains fall into *families*
that recur across neighbouring γ (GreedyCover: {0.75, 0.6, 0.5} share a
first query; γ=0.6 and γ=0.5 give the identical `k`=8 set). So "the
neighbours on both sides of 0.5" are largely the *same chain*, not new
independent draws: a bad family would show up as NO-BENEFIT across all of
it and BENEFIT below it — a STEP over γ that is equally what chain luck
produces. The design's earlier prediction ("neighbours recover under H2")
is therefore near-void on the upper side. Consequently:
- **Arm A's labels are descriptive; they never decide H1 vs H2.**
- **The verdict per algorithm is set by Arm B**, under a rule fixed now,
  before any Arm B run (paired, over the 30 resamples, both `k`=4 and 8):
  `d_r = Q_r(γ=0.25) − Q_r(γ=0.5)` with a 95 % bootstrap CI of the mean.
  **SYSTEMATIC (→ H1)** = CI excludes 0 with mean `d` > 0.02 at both `k`;
  **CHAIN-SPECIFIC (→ H2)** = CI within ±0.02 at both `k`; else
  **UNRESOLVED**. **Positive control:** the paired contrast
  `Q_r(0.25) − Q_r(1.0)` must be SYSTEMATIC at both `k`; if Arm B cannot
  reproduce the known γ=1.0-vs-γ=0.25 gap it has no power and is reported
  as uninformative. Also report the frozen full-data γ=0.5 chain's
  percentile within the resampled γ=0.5 distribution (descriptive).
- **Combination.** H1 needs Arm B SYSTEMATIC; H2 needs Arm B
  CHAIN-SPECIFIC; Arm B UNRESOLVED or failing its control ⇒
  INCONCLUSIVE for that algorithm, **whatever Arm A shows.** A monotone
  Arm A with ≥2 UNRESOLVED points between plateau and benefit region,
  plus Arm B SYSTEMATIC, is reported as **RAMP** (a graded dial), not
  INCONCLUSIVE — the earlier table had no cell for it.
- **Either way the held `METHOD.md §5.2` sentence must change** ("a clean
  continuous ablation axis" is refuted by STEP, by DIP, and by
  chain-level noise alike); what differs is the correct replacement.

---

## Required steps

**S1 — Tensor-level chain analysis (free; report before any training).**
Select chains on the full frozen tensor at a fine γ grid for both
algorithms (proposed: 0.3, 0.35, 0.4, 0.45, 0.5, 0.6, 0.75 in addition
to the existing points; TACC may adjust in Call 1, but the grid **must
include points on both sides of 0.5 at close spacing** and at least one
point ≥ 0.75). Report per chain: `k=4` and `k=8` set overlap with the
γ=1.0 chain and with the γ=0.25 chain; the per-`k` objective trace
(D012/F1 found `JointGreedy`'s objective peak moves with γ: 1/3/8/8 —
report whether it does here); and which γ's yield **identical chains**
(train once, report per γ, as D012/F2 did). This alone may show whether
selection "flips" between γ=1-like and γ=0.25-like chains at some γ.

**S2 — Arm A: fine-grid trained sweep.** Train every distinct new chain
via D012/D013's exact protocol (`hparams` hash asserted equal to D009's,
same split, same 5 seeds, `k=1..8`). Reuse existing points: `GreedyCover`
γ ∈ {1.0, 0.5, 0.25, 0.1, 0.05}; `JointGreedy` γ ∈ {1.0, 0.25, 0.1,
0.05}. (`JointGreedy` γ=0.5 is new.)

**S3 — Arm B: chain stability under resampling.** For each algorithm and
γ ∈ {1.0, 0.75, 0.5, 0.35, 0.25, 0.1} (TACC may adjust), draw `R` random
half-subsamples of the **build** configs **without replacement** (with-
replacement resampling duplicates points and biases energy distance),
re-select the chain using *only* that subsample (I2: val/test never
touched by selection), and evaluate each chain at `k=4` and `k=8` with
D008's 1-NN proxy against the full build reference. Report per γ the
distribution (median, IQR, range) of chain quality. **Proxy is for
within-algorithm ranking across γ only** — D009/R2 showed the
proxy→trained shift is condition-dependent, so never compare proxy
levels across algorithms. **Call 2:** propose `R`, and which resampled
chains to train to confirm the proxy ordering.

**S4 — Apply the pre-registered rule** (Call 1) per algorithm; state the
cross-algorithm reading.

**S5 — Write `## R`:** per-algorithm verdict, both hypotheses' dispositions
stated plainly, S1's overlap/identity findings, Arm B's distributions,
and a plain statement of what this implies for the held `METHOD.md §5.2`
sentence — **stated, not edited.**

---

## Constraints

**Applicable invariants:** I2 (Arm B selection sees only subsets of
`S_build`; val/test untouched), I4 (`GreedyCover`'s MAX machinery
unmodified; `JointGreedy` remains the parallel criterion), I5 (frozen
embeddings), I6 (all existing γ points reused; no re-generation, no
re-embedding).

**Out of scope:** editing `METHOD.md` (held by the human until this
lands); *why* a small γ helps selection in general (the project's open
question — report S1's chain-overlap and objective-trace findings if they
bear on it, but do not chase it here); Phase 3; A6.

**Two cautions that came out of D011–D013, applying here:** (i) a
reproduced anomaly on one chain is a lead, not a law — and "looks like
noise" needs the same pre-registration as "looks like signal";
(ii) a comparison's *sign* has twice been decided by a reporting
convention rather than the data (D011's threshold scale) — fix the
labelling rule first.

**Cost:** expected ~40 min of training for Arm A (≈13 new γ conditions ×
8 `k` × 5 seeds ≈ 520 runs at D012's ≈4.7 s/run) plus a few minutes of
selection and proxy evaluation for Arm B. Chain-identical γ's reduce it.
**State the measured cost in `R`.**

---

## Deliverables

| | |
|---|---|
| `results/D014/chain_analysis.json` | S1: chains, overlaps, identities, objective traces, both algorithms |
| `results/D014/metrics_by_gamma_k.json` | S2: four metrics × new γ × `k=1..8`, both algorithms |
| `results/D014/chain_stability.json` | S3: per-γ distributions of resampled-chain quality |
| `## R` below | verdicts, hypothesis dispositions, §5.2 implication |

---

## Notes for whoever writes P

- **Call 1 (numeric labelling rule and final grid) must be in P1 and
  approved before any training.** That is the whole point of
  pre-registering it here.
- **Call 2 (Arm B scope)** — `R`, γ subset, how many resampled chains to
  train. The goal is that a "STEP vs DIP" call is decidable from it, not
  that it be exhaustive.
- **Mixed outcomes are expected, not a failure** — one algorithm
  STEP, the other DIP is a real and informative result.
- **P must be executable by a fresh session with no conversation
  context**, per the standing rule in `CLAUDE.md`.

---
---

## P — Execution plan

<!-- TACC writes below this line -->

### P1

**Status: PROPOSED — awaiting review.** Full plan:
[`plans/D014-P1.md`](plans/D014-P1.md). S1 has been run (job 1007902, 1.5 min) —
D014 specifies it as free and to be reported before training. S2/S3 have not.
**No existing repo file modified.** Both Calls are answered, and Call 1's
labelling rule is fixed here, before any training, as D014 requires.

**Part 0 — S1 localises the candidate threshold for free.** The first-picked query
flips at exactly two places across the grid:

| γ | 1.0 | 0.75 | 0.6 | **0.5** | 0.45 | 0.4 | 0.35 | 0.3 | 0.25 | 0.1 | 0.05 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| first query | 123 | **117** | 117 | **117** | **114** | 114 | 114 | 114 | 114 | **193** | 193 |

**γ=0.5 is not isolated — it sits mid-family in {0.75, 0.6, 0.5}, and the family
boundary is between 0.5 and 0.45**, exactly where H1's cutoff would have to be.

**Identical `k`=8 sets**, so some training is replication rather than new
information: `GreedyCover` → {0.6, 0.5}, {0.45, 0.4, 0.35}; `JointGreedy` →
{0.45, 0.4, 0.35}, {0.3, 0.25, 0.1, 0.05} (extending D012/F2's collapse by one
point). **`GreedyCover` γ=0.6 and γ=0.5 are the same `k`=8 set**, and D012/R4
measured order-sensitivity at 0.0006 against a 0.0249 seed range — so γ=0.6 at
`k`=8 is a **near-exact replication of D013's γ=0.5 result**, built in.

**`JointGreedy`'s objective peak moves sharply with γ:** peak `k` = **1** for every
γ ≥ 0.4, then 4 / 3 / 3 / 8 / 8 at 0.35 / 0.3 / 0.25 / 0.1 / 0.05. So D012/R2's
"dimension-dominated past `k`=1" is **not a γ=1.0 property** — it holds for all
γ ≥ 0.4 and switches off between 0.4 and 0.35. `GreedyCover`'s objective is
monotone at **every** γ (all 11 checked).

**F1 — D014's independence argument is weaker than stated, and its `k`=1 part is
void.** `## D` says the algorithms "select different chains from `k`=2 on", so
chain luck is independent across them. Measured `k`=8 overlap is **2–4 of 8**
(`k`=4: 1–2 of 4) — partly independent, but two greedy walks over the *same frozen
tensor*, so a query genuinely strong under a given γ tends to be picked by both.
And **at `k`=1 the two algorithms are the same algorithm** (D010/R2, D013/F3), so
the shared first-query flips above are **one observation, not two**. The design is
still the best test available; **proposal: report the cross-algorithm reading using
`k` ≥ 2 only**, with the overlap table beside it, and call agreement *evidence
against* chain luck rather than a refutation of it.

**F2 — a pre-registered discriminator.** The first-query family boundary is at
**0.5/0.45**; `JointGreedy`'s objective-peak transition is at **0.4/0.35**. They do
not coincide. **If training puts the BENEFIT/NO-BENEFIT boundary at 0.5/0.45, γ
acts through which queries get picked first; if at 0.4/0.35, through the
objective's shape.** Stated now so it cannot be constructed afterwards.

**F3 — I retract D013/R2's mechanism.** `## D` is right that it predicts the wrong
thing: "`CVaR₀.₅` commits to neither mean nor tail" implies performance *between*
γ=1 and γ=0.25, but D013 measured γ=0.5 at or slightly *below* γ=1.0 (worst-class:
below γ=1.0 at 6 of 8 `k`). **A mechanism that predicts the wrong sign is wrong,
not weak.** `R` will retract it explicitly. I have no replacement and am not
inventing one.

**Call 1 — grid as proposed; labelling rule fixed now.** Per `k`: *better* = CI
excludes 0 **and** |Δ| exceeds the 5-seed run range; *equivalent* = TOST at δ=0.02;
else unresolved. Over `k`=1..8: **BENEFIT** = better at ≥5 of 8 and never worse
anywhere; **NO-BENEFIT** = better at ≤1 of 8; **UNRESOLVED** otherwise. The
cut-points are calibrated to reproduce the labels the existing data already
implies — D013's γ=0.1 (6/8) and γ=0.05 (7/8) BENEFIT, γ=0.25 (5/8) BENEFIT at the
boundary, γ=0.5 (0/8) NO-BENEFIT — which is the point of fixing them before
training.

**Call 2 — R = 30 half-subsamples, γ ∈ {1.0, 0.75, 0.5, 0.35, 0.25, 0.1}, both
algorithms, proxy only, no Arm-B training.** 30 gives a real IQR where D008/R5 and
D010/S6b each had a spread of two. Arm B's question is a *within-γ, within-algorithm
ranking*, exactly what D009/R2 licensed the proxy for; training 180 extra chains to
re-derive a ranking the proxy already gives would cost ~4× Arm A for no new
inference. **If a training check is wanted, the cheapest sufficient one is the best
and worst resampled chain at γ=0.5 for `GreedyCover` — 10 runs — and I will add it
on request.**

**Cost:** Arm A **≈41 min** (13 new conditions × 8 `k` × 5 seeds = 520 runs at
D012's measured 4.7 s/run); Arm B a few minutes; S1 cost 1.5 min. I am **not**
skipping runs on the identical-set collapses — the chains differ at `k`<8, and the
`k`=8 duplicates are the replication checks Part 0 and F1 rely on.

**Sequencing:** S2/S3 depend on Calls 1–2; nothing else is blocked.

## Review — D014 / P1

### For you (human)

**Verdict:** APPROVED WITH AMENDMENTS

**What changed:**
- **Call 1 (labelling rule) approved as TACC fixed it**, with one
  annotation request: also flag "HARM" (worse than γ=1.0 at ≥3 of 8 `k`)
  beside the label, since D013 saw three intervals excluding zero in the
  *worse* direction at γ=0.5 and the label alone would hide that.
- **Call 2 (Arm B) approved as proxy-only, R = 30** — plus two things I
  added: a **numeric, paired decision rule for Arm B**, fixed now before
  any Arm B run (see `## D`'s amendment), and a **20-run trained sanity
  check** (proxy-best and proxy-worst resampled chain at γ=0.5 and γ=0.25,
  `GreedyCover`, `k`=8, 5 seeds each) to confirm the proxy can rank chains
  *within* a γ, which D009's cross-condition validation did not test. If
  the trained ordering disagrees in sign at either γ, Arm B is reported as
  proxy-level and unconfirmed.
- **F1 accepted — and it retracts something I told you.** I said the two
  algorithms' chain luck is independent, making cross-algorithm agreement
  an independent test. It is only partly independent: both walk the same
  noisy tensor. Agreement counts as *evidence against* luck, read at
  `k` ≥ 2 only. `## D` carries the correction.
- **A flaw in my own D014 design, found via P1's S1:** chains recur as
  *families* across neighbouring γ, so the grid points on either side of
  0.5 are mostly the same chain — Arm A cannot tell "real threshold" from
  "one unlucky chain". **Arm B is now the deciding evidence, Arm A
  descriptive**; the earlier outcome table also had no cell for a graded
  dial, now added (RAMP).
- **F2 and F3 accepted.** F3 is TACC retracting its own D013 mechanism
  because it predicts the wrong sign — noted, no action needed.

**Needs your input:** None. Nothing here is a value trade-off; the items
above are decided on methodological grounds and listed so you can veto
any of them. The `METHOD.md §5.2` hold stands.

**Follow-ups opened:** None.

**Next:** TACC runs S2 (fine-grid training, ≈41 min) and S3 (Arm B plus the
20-run check) under the amended rule.

---

### Technical log

**Why chain families defeat Arm A.** S1: first query is 123 at γ=1.0, 117
for {0.75, 0.6, 0.5}, 114 for {0.45…0.25}, 193 for {0.1, 0.05}; GreedyCover
γ=0.6 and 0.5 select the identical `k`=8 set, as do {0.45, 0.4, 0.35}.
Under chain luck, an unlucky family {0.75, 0.6, 0.5} yields NO-BENEFIT on
all three and BENEFIT below — a STEP. Under a systematic γ effect the same
labels appear. The pattern is identical, so it cannot decide. Only
perturbing the data the chains are built from (Arm B) separates them.
S1 is otherwise neutral on H1/H2; the ~60/40 lean toward H2 is unchanged.

**Why Arm B is also the better answer to the shared-noise problem in F1.**
The two algorithms share the tensor, so their agreement is confounded by
shared estimation noise; resampling the build configs perturbs exactly
that shared noise. A paired contrast within each resample removes
resample-level difficulty, which a comparison of marginal distributions
would not.

**The numeric thresholds** reuse the project's own bound (`δ` = 0.02,
"the smallest difference this project has resolved") rather than
inventing one; the positive control (γ=1.0 vs 0.25) guards against an
underpowered Arm B being read as "chain-specific" when it is merely blind.

**F2's discriminator** (boundary at 0.5/0.45 ⇒ early picks; at 0.4/0.35 ⇒
objective shape) is a sound pre-registration but it does *not* separate H1
from H2 — discrete chain families appear under both. It also applies
differently by algorithm: `GreedyCover`'s objective is monotone at every
γ, so "objective shape" is unavailable to it; only `JointGreedy`'s
objective-peak transition (γ ≥ 0.4 peaks at `k`=1) can be tested against
it. A boundary for `GreedyCover` at 0.4/0.35 would be unexplained by
either reading and should be reported as such.

**Cost.** ≈41 min Arm A + a few minutes Arm B + ~2 min for the sanity
check; guardrail 3 GPU-hours. Running the identical-set duplicates is
right: γ=0.6 at `k`=8 is a free replication of D013's γ=0.5 (same chain —
a replication of the measurement, not independent evidence about γ).

## R — Results summary

<!-- TACC writes below this line -->

# **`JointGreedy`: RAMP (H1 — systematic). `GreedyCover`: INCONCLUSIVE — Arm B failed its own control.**

Per the 2026-09-19 amendment, **Arm B decides and Arm A is descriptive.** Arm B's
pre-registered rule, applied without adjustment:

| | main contrast `Q(0.25) − Q(0.5)` | positive control `Q(0.25) − Q(1.0)` | verdict |
|---|---|---|---|
| **`JointGreedy`** | `k`=4 **+0.0852** [+0.0808, +0.0897] · `k`=8 **+0.0365** [+0.0309, +0.0420] | `k`=4 +0.1073 · `k`=8 +0.1036 — **SYSTEMATIC** | **SYSTEMATIC (→ H1)** |
| **`GreedyCover`** | `k`=4 +0.0245 [+0.0093, +0.0403] · `k`=8 **+0.0149** [+0.0075, +0.0222] | `k`=4 **+0.0125** [−0.0031, +0.0295] — **fails** | **UNINFORMATIVE** |

30 half-subsamples of the 75 build configs, without replacement; chains
re-selected on the subsample only; scored by D008's 1-NN proxy on **`val`** with
the full build reference — so Arm B never touches `test` (I2) and is independent
of Arm A's reported numbers.

### R1 — `JointGreedy`: the γ effect is systematic, and it is a RAMP, not a step

Arm B separates γ=0.25 from γ=0.5 decisively (+0.085 at `k`=4, CI nowhere near
zero) and its control reproduces the known γ=1.0-vs-0.25 gap at +0.104, so it has
power. **H1 holds; H2 is rejected for this algorithm.**

Arm A's labels — descriptive only — show the shape:

| γ | 0.75 | 0.6 | 0.5 | 0.45 | 0.4 | 0.35 | 0.3 | 0.25 | 0.1 | 0.05 |
|---|---|---|---|---|---|---|---|---|---|---|
| label | NO-BEN | NO-BEN | **UNRES (2/8)** | **UNRES (4/8)** | BEN (5/8) | BEN (6/8) | BEN (7/8) | BEN (7/8) | BEN (8/8) | BEN (8/8) |
| mean top-1, `k`=8 | 0.8210 | 0.8182 | 0.8437 | 0.8711 | 0.8718 | 0.8709 | 0.8776 | 0.8763 | 0.8757 | 0.8763 |

**Two UNRESOLVED points sit between the plateau and the benefit region, and the
per-`k` "better" count climbs 0 → 2 → 4 → 5 → 6 → 7 → 8 monotonically.** That is
exactly the cell the amendment added: **RAMP — a graded dial, not a step.** Arm B
SYSTEMATIC + ≥2 UNRESOLVED points between the regions is the amendment's own
definition, met precisely.

### R2 — `GreedyCover`: the experiment did not have power, and the rule says so

Arm B's control — which must reproduce a gap D009/D013 already measured as real —
comes back **+0.0125 [−0.0031, +0.0295] at `k`=4**: CI spans zero and the mean is
below the 0.02 margin. **By the pre-registered rule the whole arm is uninformative
for this algorithm, whatever the main contrast shows**, and I am reporting it that
way rather than quoting the `k`=8 numbers where it did have power.

Worth stating precisely, because it is nearly a result: **at `k`=8 the control
passes (+0.0405 [+0.0343, +0.0468]) and the main contrast is +0.0149
[+0.0075, +0.0222] — CI excludes zero but the mean falls below the 0.02
margin, i.e. UNRESOLVED even there.** So on the half-data resamples `GreedyCover`'s
γ=0.25-vs-γ=0.5 gap is real but small. The honest summary is *"too small to
classify at this sample size"*, not *"absent"* and not *"present"*.

**Why the two algorithms differ in power is visible in the distributions.** At
`k`=8, `JointGreedy`'s medians march 0.7124 → 0.7254 → 0.7784 → 0.7957 → 0.8205 →
0.8259 across γ = 1.0 → 0.1 with tight IQRs; `GreedyCover`'s go 0.7319 → 0.7389 →
0.7546 → 0.7600 → 0.7773 → 0.7859 — **half the total span, with overlapping
IQRs.** Half-data selection simply moves `GreedyCover` less.

### R3 — Arm A was worth running, and it was not decisive, exactly as the amendment predicted

`GreedyCover`'s Arm A labels are **NO-BENEFIT at every γ from 0.75 down to 0.3, then
BENEFIT at 0.25** — a clean-looking step. **Had the amendment not ruled Arm A
descriptive, this D would have reported STEP for `GreedyCover` on evidence that
cannot distinguish a threshold from one unlucky chain family.** S1 showed γ ∈
{0.45, 0.4, 0.35} are the *identical* `k`=8 set, so those three "independent"
NO-BENEFIT labels are one chain counted three times.

That is the amendment earning its place, and it is the second time in three D's
that a labelling or scoping decision, not the data, would have set the reported
sign (D011's threshold scale was the first).

### R4 — F2's discriminator: the boundary matches the **objective**, not the chain family

Pre-registered in P1 before training: the chain-family boundary is at **0.5/0.45**;
`JointGreedy`'s objective-peak transition is at **0.4/0.35**; whichever the
BENEFIT boundary matches tells you what γ acts through.

`JointGreedy`'s first BENEFIT label is at **γ=0.4**, and its peak-`k` leaves 1 at
**γ=0.35**. Neither boundary is clean — 0.5 and 0.45 are UNRESOLVED, straddling
both candidates. **The discriminator does not resolve.** With a RAMP rather than a
step there may be no single boundary to match, which is a coherent reading but not
one I can claim from these numbers. Reported as **not resolved**, per the rule I
fixed in advance.

### R5 — The proxy check passed, and it tested something D009 never did

The review's 20-run check: proxy-best vs proxy-worst resampled chain, trained.

| | proxy | trained | agrees |
|---|---|---|---|
| γ=0.5 best / worst | 0.7946 / 0.7351 | 0.8525 / 0.8022 | ✅ |
| γ=0.25 best / worst | 0.7989 / 0.7459 | 0.8655 / 0.8084 | ✅ |

**The proxy ranks chains correctly *within* a γ** — D009 validated it across
conditions, never within one, which is precisely what Arm B asks of it. Arm B's
numbers are confirmed at the trained level, not merely proxy-level.

### R6 — Two disclosures

**A bug in my own reuse map, caught and fixed without retraining.** Arm A's first
pass left `JointGreedy` γ=0.1 blank. Cause: D012 itself *read* that point from D010
under I6 and therefore stored no per-config counts, so `results/D012/run_counts.npz`
holds only {1.0, 0.25, 0.05}. My map pointed at D012 for a cell that lives in D010.
Fixed in `experiments/d014_relabel.py`, which re-derives the labels from the
already-saved runs — **nothing was retrained.** The blank cell is now BENEFIT (8/8),
the strongest label in the grid, so it also did not change any conclusion.

**A cost correction.** P1 said Arm B would take "a few minutes". The first
implementation rebuilt the 172k-cell energy tensor per resample — ~4 min each,
~2 h for R=30. It was redundant: scale-free energy is recoverable from the
accumulator blocks the `JointGreedy` precompute already builds, so `Ssub` costs two
sqrt-means. **Arm B ran in 9.9 min.** Arm A took **38.9 min** against P1's ≈41 min
estimate.

### R7 — Verdicts, hypotheses, and `METHOD.md §5.2`

| | |
|---|---|
| **`JointGreedy`** | **RAMP (H1).** Arm B SYSTEMATIC with a passing control; Arm A monotone with 2 UNRESOLVED points between regions. |
| **`GreedyCover`** | **INCONCLUSIVE.** Arm B failed its control at `k`=4; at `k`=8 the gap is real but below the 0.02 margin. |
| **H1** | Supported for `JointGreedy`; untested-in-effect for `GreedyCover`. |
| **H2** | **Rejected for `JointGreedy`.** Not rejected for `GreedyCover`, but not supported either. |
| **Design-side's 60/40 lean toward H2** | Wrong for the algorithm where the question could be answered. Recorded as the D asked. |

**On the held `METHOD.md §5.2` sentence — stated, not edited.** *"γ = 1.0 recovers
the mean objective … which makes γ a clean continuous ablation axis"* should not
stand. But the correct replacement is **not** "γ has a threshold": for
`JointGreedy`, the one algorithm where this D had power, benefit rises **gradually**
across 0.5 → 0.4 → 0.35 → 0.3. A defensible replacement on current evidence is that
γ is a **continuous but strongly non-linear** axis — most of the benefit is
realised below γ ≈ 0.4, with little to gain from going below ≈ 0.25 (D012/R3 and
D013/R3 both found no ordering among γ ≤ 0.25) — and that **`GreedyCover`'s fine
structure is not established.**

Cost: S1 1.5 min · Arm A 38.9 min · Arm B 9.9 min · relabel <1 min. **≈51 minutes.**
No existing repo file modified; new files only.

### R8 — For design side

1. **The RAMP cell the amendment added is the one that fired.** Without it this
   would have been INCONCLUSIVE for `JointGreedy` on a monotone, well-powered
   result.
2. **`GreedyCover`'s fine γ structure is open**, and the reason is measurable:
   half-data resampling moves its chain quality half as much as `JointGreedy`'s.
   Powering it would need more resamples or full-data perturbation of a different
   kind — worth a sentence in `PLAN.md`'s open items, not a new D on my say-so.
3. **D013/R2's γ=0.5 claim needs softening in light of this.** It reported γ=0.5 as
   a property of γ; Arm A here reproduces the pattern, but Arm B cannot confirm the
   mechanism for `GreedyCover` at all. The `## R` there should carry a pointer.

---
---

