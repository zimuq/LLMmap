# D017 — Does the selection benefit survive a linear classifier, or is it entangled with the paper's attention network?

```
id:          D017
source:      Human proposal, this session (2026-09-24): after D015/D016
             established that joint energy's advantage concentrates on
             hard (structural near-relative) pairs/models, test whether
             that advantage — and the aggregate mean-top1 advantage — is
             a property of the SELECTED QUERIES (should survive any
             reasonable downstream classifier) or is entangled with the
             paper's own attention-based stage-2 network (`METHOD.md
             §5.5`'s trained pipeline). Human-approved 2026-09-24
             ("先起草D017").
status:      OPEN
gate:        REVIEW          # a new classifier architecture is a real
             experiment producing numbers that could feed a writeup
             claim (the theoretical framing, METHOD §3-4) — default per
             CLAUDE.md
deps:        D007 (frozen S_energy tensor, I6), D008 (near_relative_pairs,
             hard-pair machinery in `d008_lib.py`), D009 (paper8/coverage
             embeddings, traces, splits, and the attention-network
             baseline numbers this D compares against), D010
             (joint_energy chain), D015 (per-pair extraction pattern),
             D016 (confirms the 65 structural pairs are the complete hard
             population — used here only as background; this D does NOT
             reopen the hard-pair-definition question, see Constraints) —
             all CLOSED.
opened:      2026-09-24
closed:      —
downstream:  Bears directly on `DECISIONS.md` D5's claim (3) — theoretical
             formalization — by testing whether the theory's classifier-
             agnostic prediction (benefit lives in the data/query
             information content, not a specific network's inductive
             bias) holds empirically. Independent of the still-open
             hard-pair-definition thread (explicitly not touched here).
```

---

## D — The question

> **Train a simple linear classifier — not the paper's attention network
> — on the exact same frozen traces, splits, and already-selected query
> chains, and ask: does `joint energy`'s advantage over `paper8` (both
> the aggregate mean-top1 gain and its concentration on hard
> near-relative pairs/models) survive, or is it specific to the
> attention network's capacity/inductive bias?**

### Why this is worth the (cheap) cost

`METHOD.md §3–4`'s theoretical framing is about the *information content*
of the selected queries' responses (an MI decomposition over a
point-cloud separability statistic) — a claim that should not depend on
which downstream classifier reads that information out. If a minimal
linear classifier shows the same qualitative story (joint energy >
coverage > paper8 in ordering, and a hard-pair-specific amplification),
that is direct empirical support for "the benefit is in the data," and
strengthens `DECISIONS.md` D5's claim (3) beyond an aggregate-accuracy
argument alone. If it does not survive, that is equally important to know
before any writeup leans on the theoretical framing — and per D009's own
precedent, a "FALSIFIED" branch here is not a failed D, it is the answer.

**Cost is the reason to do this now rather than later.** A linear
classifier is far cheaper to fit than the attention network (no backward
passes through an attention stack, likely sub-second per fit) — this D is
expected to be one of the cheapest in the project, cheaper than D015/D016
even.

### What is and is not known going in

- The attention network's input is already `[E(query); E(response)]`,
  2048-d per trace (`METHOD.md §5.5`) — **no new embeddings are needed**;
  the same frozen per-trace feature vectors this project has used since
  D009 are the linear classifier's input too.
- The attention network takes a *set* of `k` such vectors and
  self-attends across them; D012/R4 confirmed it is **insensitive to
  slot order** (a 40×-smaller-than-seed-noise effect). A linear model
  cannot consume a variable, unordered set directly — **it needs a
  pooling decision**, which does not exist yet and is this D's first
  required design call (S0/Call 1).
- **It is not known whether a linear classifier is stochastic in this
  setting at all.** The attention network's "5 seeds" captures training
  (init + shuffling) stochasticity; a convex linear model fit with a
  deterministic solver (e.g. L-BFGS) may have *no* seed variance to speak
  of. This changes what "resolved" means here and must be checked (S0),
  not assumed either way.
- **Not reopening the hard-pair-definition thread.** D015/D016/this
  session's chat explored several candidate "hard" populations (the 65
  structural pairs; several data-driven sub-clusters within it) — that
  question is explicitly parked. This D uses only two already-settled,
  uncontroversial reference points for its hard-pair check: the full
  65-pair structural set (`hard_subset`, as currently defined in
  `METHOD.md §8`) and the two pairs named since D001 (Falcon3-10B/7B,
  Phi-3-medium-128k/4k). No new cutoff is introduced here.

---

## What counts as an answer

Report, for `paper8`, `coverage` (γ=0.1), `joint energy` (γ=0.1), at
`k`=1..8 (cheap enough to do the full curve, matching this project's
standing practice since D008):

- **mean top-1, worst-class, `hard_subset`** (the 65-pair set, unchanged
  definition) under the linear classifier — the same three metrics D009
  onward report for the attention network, enabling a direct side-by-side
  table.
- **The two named pairs' own two-logit-restricted accuracy** under the
  linear classifier, compared to their known attention-network values
  (Falcon3-10B/7B: .800/.800/.832; Phi-3-medium-128k/4k: .712/.656/.732
  for paper8/coverage/joint energy at `k`=8).
- **Whether the ordering `joint energy > coverage > paper8`** (on mean
  top-1, and on the two named pairs) **is preserved, reversed, or
  statistically indistinguishable** under the linear classifier — state
  this per `k`, not just at `k`=8, and require a *consistent sign across
  k* before calling an ordering preserved (this project's standing
  convention, e.g. D010/D012).

**Three symmetric, equally informative outcomes — pre-registered before
any run, per standing project discipline:**

| outcome | reading |
|---|---|
| **CLASSIFIER-AGNOSTIC** | Ordering and hard-pair concentration both preserved (consistent sign across `k`, CI excludes zero where the attention-network version was resolved) → strong support for D5 claim (3); the benefit is in the selected data, not the network |
| **CLASSIFIER-DEPENDENT** | Ordering preserved on aggregate mean top-1 but the hard-pair-specific amplification shrinks to statistical indistinguishability (TOST, δ=0.02) or reverses → the aggregate story is robust, the hard-pair story is not; report both halves separately, do not average them into one verdict |
| **FALSIFIED** | Ordering itself reverses or is indistinguishable from noise on mean top-1 → the whole selection-algorithm advantage may be entangled with the attention network's capacity; **not a failed D** — exactly as important as the other two branches, and it would need a design-side/human conversation before any further writeup leaning on classifier-agnosticity |

**Not required:** extending to D012/D013's γ grid, or to more than the
three headline conditions — flag as a cheap follow-up if `k`=8's result
is informative enough to want more. **Not required:** resolving which
"hard" cutoff (the parked question) is correct — use only the two
reference points named above.

---

## Required steps

**S0 — Design and sanity-check the pooling + determinism, before any
grid.** **Call 1 (state in P1, get it right before training anything):**
how does a linear classifier consume `k` unordered `[E(q);E(r)]`
vectors? Proposal to evaluate: **mean-pool across the `k` slots, then one
multinomial (37-way) logistic regression layer** — preserves the
attention network's confirmed order-insensitivity, simplest defensible
choice. State any alternative considered (e.g. concatenation, which would
reintroduce a slot-order dependency the attention network provably does
not have) and why it was rejected, or why mean-pooling was accepted
without one. **Also determine empirically whether the fit is
deterministic** (e.g., L-BFGS on a convex objective with fixed data
typically is) — fit `paper8` `k`=8 twice, same command, and report whether
the two fits are bit-identical. If deterministic, say what replaces "5
seeds" as this D's source of reported uncertainty (proposal: the same
config-level bootstrap this project already uses for CIs, which does not
depend on training-seed variance and is already the primary uncertainty
source in every prior D's "resolved" calls) — if stochastic, keep 5 seeds
and say why.

**S1 — Fix the regularization/hparams once, not per condition.** Pick
any regularization strength (or lack of one) via the validation split on
one representative condition (`paper8`, `k`=8), then **freeze it and
reuse identically across every condition/`k`** — the same fairness
discipline as the attention network's asserted-identical `hparams` hash
(D009/F4's precedent: an unequal classifier per condition would leak an
incidental advantage into the comparison this D exists to make cleanly).
State the chosen value and the validation-accuracy curve that picked it.

**S2 — Train/fit the linear classifier for `paper8`, `coverage` (γ=0.1),
`joint energy` (γ=0.1), `k`=1..8**, same split, same frozen features,
same fixed hparams (S1). Measure and report wall time before committing
to the full grid, per standing practice (D002/D006/D007/D009/D010/D012's
precedent).

**S3 — Report the four `METHOD.md §8` metrics** (mean top-1, worst-class,
`hard_subset`, and — since selection cost is unaffected by which
classifier reads the result — no new query-efficiency computation needed)
for every condition/`k`, in the same shape as D009/D010's tables, for
direct comparison.

**S4 — The two named pairs' accuracy under the linear classifier**, same
computation D015/D016 already used (`logit_stats`'s two-logit restriction,
here on the linear model's own logits), at `k`=8 (and `k`=1 for the same
contrast D015 reported). Compare directly against the already-known
attention-network values quoted above.

**S5 — State the verdict against the three-way outcome table above**,
per metric, with sign-consistency-across-`k` as the ordering criterion and
TOST (δ=0.02) for any "statistically indistinguishable" call, matching
this project's standing conventions exactly (no new statistical framework
introduced for this D).

---

## Constraints

**Applicable invariants:** I2 (identical train/val/test split reused,
`hparams` frozen once per S1, not tuned per condition), I5 (frozen
embeddings — no new embedding model, no re-embedding), I6 (same frozen
corpus/tensor and the *already-selected* query chains from D009/D010 —
no new selection, no new generation).

**Out of scope:** re-litigating which pairs/models count as "hard"
(explicitly parked — use only the 65-pair `hard_subset` and the two named
pairs); any new query selection or γ value; editing `METHOD.md` or
`DECISIONS.md` D5/D7 (this D's `## R` informs both, decides neither);
extending beyond the three headline conditions without a design-side
Call.

**Cost:** expected to be one of the cheapest D's in the project —
sub-second fits, no backward pass through an attention stack. **State the
measured cost in `R`** regardless, per standing practice.

---

## Deliverables

| | |
|---|---|
| `results/D017/pooling_and_determinism_check.json` | S0 |
| `results/D017/hparam_selection.json` | S1 |
| `results/D017/linear_metrics_by_k.json` | S2/S3: four metrics × 3 conditions × `k`=1..8 |
| `results/D017/linear_named_pairs.json` | S4 |
| `## R` below | S5's verdict, and the direct linear-vs-attention comparison table |

---

## Notes for whoever writes P

- **Call 1 (pooling strategy) and the determinism check must be in P1 and
  settled before any training** — this is where a wrong default (e.g.
  concatenation reintroducing an order dependency) would quietly bias
  everything downstream.
- **Do not tune the linear classifier separately per condition** — S1
  exists specifically to prevent an unequal-classifier confound from
  entering the one comparison this D is built to make cleanly (same
  concern D008/P1's F4 raised for the original network's distance
  scaling).
- **A FALSIFIED or CLASSIFIER-DEPENDENT outcome is not a worse result
  than CLASSIFIER-AGNOSTIC** — report whichever the data show, per this
  project's standing discipline (D009's own precedent for treating a
  proxy failing to predict trained results as the most important possible
  outcome, not a disappointing one).
- **P must be executable by a fresh session with no conversation
  context**, per the standing rule in `CLAUDE.md`.

---
---

## P — Execution plan

<!-- TACC writes below this line -->

### P1

**Status: PROPOSED — awaiting review.** Full plan:
[`plans/D017-P1.md`](plans/D017-P1.md). S0 and S1 have been run (jobs 1022365,
1022392 — **3.4 min**), since D017 requires both settled before any grid. S2–S5
have not. **No existing repo file modified.**

**Call 1 — concatenation, not mean-pooling. D017's stated objection does not hold
for a *fitted* model.** D017 asks that concatenation be rejected because it *"would
reintroduce a slot-order dependency."* For a fitted linear model
`f(x) = Σᵢ Wᵢvᵢ + b`, permuting slots permutes the learned blocks, and both loss and
L2 penalty are permutation-equivariant, so the convex optimum is order-free. **My
first measurement contradicted that** (0.900541 chain vs 0.896216 permuted), so I
tested whether it was the objective or the solver:

| solver | chain order | permuted | gap |
|---|---|---|---|
| `max_iter=3000, tol=1e-4` | 0.900541 | 0.896216 | **0.004324** |
| `max_iter=20000, tol=1e-6` | 0.902703 | 0.902703 | **0.000000** |
| `max_iter=60000, tol=1e-8` | 0.902703 | 0.902703 | **0.000000** |

**An lbfgs convergence artifact; it closes to exactly zero.** Right about the
objective, wrong about the default solver settings — recorded that way.

Concatenation is also the more faithful analogue: the attention network *can* tell
which query produced which response, concatenation can (separate weight block per
slot), **mean-pooling cannot** — it is the strict special case `Wᵢ = W/k`. Measured
at `paper8` `k`=8: **concat 0.9005 vs mean-pool 0.8281**. So mean-pooling alone
would have understated every condition by ~7 points. **Proposal: concat primary,
mean-pool secondary** — the gap between them measures how much of the benefit needs
per-query resolution, which is D017's theoretical question in miniature. Solver
frozen at `max_iter=20000, tol=1e-6`.

**Part 0(a) — `E(query)` contributes nothing to any linear fit here.** Within a
condition it is **identical across all 2,775 training traces** (exact), so `Wᵍ·E(q)`
is a constant absorbed into the bias. Confirmed: full 2048·`k` scores 0.900541,
response-only 1024·`k` scores 0.899459. The "2048-d trace" is effectively 1024-d per
slot for this D. The attention network can use `E(query)` to modulate how each slot
is read; concatenation reproduces that via separate blocks, mean-pooling cannot —
which is the mechanism behind the 7-point gap. Proposal: keep the full input for
faithfulness, and say this in `R` so nobody reads the query half as doing work.

**Part 0(b) — the linear classifier BEATS the paper's attention network.**

| `paper8`, `k`=8 | mean top-1 |
|---|---|
| attention network (D009, same chain, seed 0) | 0.8346 |
| linear, mean-pool | 0.8281 |
| **linear, concatenation (C=1.0, converged)** | **0.9027** |

**+6.8 points over the architecture the paper specifies**, on identical frozen
traces, splits and chain. This does not change what D017 asks, but it changes the
frame: the D is written as *"does the advantage survive a simpler/weaker
classifier"*, and **the premise that a linear model is weaker is wrong at this data
scale** (2,775 samples against 3.0M attention parameters). **Proposal: report it as
linear-vs-attention, not weaker-vs-stronger**, and flag for design side that the
attention network may be undertrained or overparameterised here — a separate
question this D does not answer and must not be read as answering.

**S1 — C picked once on validation, frozen everywhere.** Grid 0.003→3.0 on
`paper8` `k`=8, val only: 0.8778 / 0.9027 / 0.9114 / 0.9146 / 0.9146 / **0.9189** /
0.9189. **Chosen C = 1.0** (ties with 3.0; smaller of the tie = more
regularization). Mean-pool's own sweep picks **C = 3.0** so the secondary is not
handicapped by a constant tuned for a different feature space. Test was computed in
the same pass but **not used to select** — disclosed because both argmaxes coincide,
which is a coincidence and not a procedure.

**F1 — the fit is bit-deterministic across `random_state`, so "5 seeds" measures
nothing.** Proposal: drop the seed axis for linear conditions and report the
config-level paired bootstrap only (D017/S0 anticipated this). **Consequence stated
plainly:** the attention-network numbers being compared against *do* have a seed
axis, so a cross-architecture delta has a seed range on one side only — those will
be labelled *"resolved against the attention network's own seed variability"*, not
as a symmetric test.

**F2 —** `coverage` and `joint_energy` coincide at `k`=1 (D015/R5, D010/R2), so
that column is one condition, not two agreeing ones. Asserted as an end-to-end
check and marked in `R`.

**F3 —** cost is **≈15 min**, not the "sub-second fits" D017 expects: a converged
`k`=8 concat fit is 32 s (16,384 features × 37 classes). Still cheap; stated now.

**Environment note, not a code change:** `sklearn` 1.9 removed
`LogisticRegression(multi_class=...)` — lbfgs is multinomial by default for >2
classes, so the argument was dropped. Cost one failed job (1022355).

**Sequencing:** S2–S5 depend on Call 1 only. S0 and S1 are complete.

---
---

## Review — D017 / P1

### For you (human)

**Verdict:** APPROVED WITH AMENDMENTS. Call 1 (concatenation instead of
my proposed mean-pooling) and F1–F3 all accepted — TACC's reasoning is
right and my original default was the weaker choice; said so plainly
below. No blocking issues. One finding needs your attention even though
it doesn't block execution.

**What changed:** D017 proposed mean-pooling to preserve the attention
network's known order-insensitivity; TACC showed that concern doesn't
apply to a *fitted* linear model (permuting slots just permutes which
learned weight block multiplies which slot — the fitted function is
provably unchanged) and verified it empirically (an apparent
order-dependence traced to an under-converged solver, closes to exactly
0.000000 with tighter settings). Concatenation is adopted as primary,
mean-pooling kept as a secondary point of comparison.

**The finding worth your attention, unrelated to whether Call 1 is
right:** on `paper8` `k`=8, the linear classifier scores **0.9027** —
**6.8 points above the paper's own attention network (0.8346)**, same
frozen chain, same split. D017 was framed as "does the benefit survive a
*simpler/weaker* classifier" — that framing no longer fits; on this
corpus size (2,775 training traces vs. the network's ~3.0M parameters),
the linear model is not the weaker baseline. This doesn't block S2–S5
(the actual question — does `joint energy`'s advantage hold under this
classifier — is unaffected either way), but it is a citable, surprising
result in its own right, and it's worth deciding now whether you want it
called out as its own point in a writeup, or investigated further
(e.g., whether the attention network is undertrained/under-regularized
at this sample size) — TACC correctly declined to answer that question
here since D017 doesn't cover it.

**Needs your input:** Optional, not blocking — see above. If you want
this pursued, it's a separate D, not an amendment to this one.

**Follow-ups opened:** None as a new D yet.

**Next:** TACC executes S2–S5 on the concatenation-primary basis.

---

### Technical log

**Call 1 verification.** Reproduced from `results/D017/s0_log.txt` and
`s0b_s1_log.txt` exactly: default-solver gap 0.900541 vs 0.896216
(0.004324); at `max_iter=20000, tol=1e-6` both converge to **0.902703,
gap 0.000000**. The theoretical argument (permutation-equivariance of a
convex fit) is correct and the empirical trace — apparent violation,
traced to solver non-convergence rather than dismissed or accepted at
face value, then confirmed closed — is exactly the right way to handle a
result that contradicts a prediction. Approved.

**Part 0(a) — `E(query)` contributes nothing.** Verified
(`pooling_and_determinism_check.json`: `constant_across_traces: true`) —
correct and mechanically obvious in hindsight (a condition's chain is one
fixed query per slot for every trace, so the query-half of the
concatenated feature has zero within-condition variance and is absorbed
into the bias). Correctly flagged so `R` doesn't let a reader think the
query embedding is doing discriminative work in this specific linear
setup.

**Part 0(b) — verified, and flagged for you above rather than buried.**
`0.9027` reproduces `results/D017/hparam_selection.json`'s `C=1.0` test
value exactly; `0.8346` matches the attention-network value already
independently verified in D016's review. Sample-size asymmetry (2,775
traces vs. ~3.0M parameters) is a plausible, honest explanation for why a
regularized linear model could win here — TACC named it as a hypothesis,
not a conclusion, and correctly declined to chase it inside this D's
scope. Right call.

**F1 — determinism, verified independently:** `coef_bit_identical_across_seeds:
true, max_coef_diff: 0.0`. Dropping the seed axis for linear conditions
and relying on the config-level bootstrap is the correct adaptation — the
project's uncertainty convention was always config-bootstrap-primary,
seed-range was a secondary check layered on top for the neural network's
training stochasticity specifically, which does not exist here. Approved,
including the asymmetric-labelling caveat for any linear-vs-attention
delta (one side has seed variance, one does not) — that caveat should
appear next to any such comparison in `R`, not just here.

**F2, F3 — accepted as stated**, both consistent with already-established
project facts (D010/R2, D015/R5 for F2; a corrected but still-cheap cost
estimate for F3).

**No amendments to S2–S5 as specified in D017**, beyond running them on
the concatenation-primary, mean-pool-secondary basis Call 1 settles.
Proceed.

---

## R — Results summary

<!-- TACC writes below this line -->
