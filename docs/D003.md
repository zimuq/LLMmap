# D003 — Can a proxy LLM generate the candidate query pool Q_0 needed for the mean-vs-CVaR comparison?

```
id:          D003
source:      ADDENDUM.md item 2 (design discussion, not yet formalized before this)
status:      OPEN
gate:        REVIEW          # plan must be approved before execution
opened:      2026-09-01
closed:      —
downstream:  Feeds C3 (initial pool size, DECISIONS.md) and Phase 1 corpus
             construction (METHOD.md §5.4, PHASE 0). Does NOT depend on D001 or
             D002 and can run in parallel with both — but the corpus-building
             step that *consumes* D003's output does depend on A1 (needs D001)
             and C3 sizing (needs D002).
```

---

## D — The question

> **Can a proxy LLM, prompted per query-family with the paper's original 8
> queries and published baselines seeded in as anchors, produce a ~50–100-item
> candidate query pool suitable for Phase 1 — without hand-curation, and
> without the low-diversity failure mode where a bad pool makes every
> selection algorithm look equally mediocre?**

### Why this gates the mean-vs-CVaR comparison, not just Phase 1's convenience

The comparison that justifies CVaR-coverage over mean-greedy (`METHOD.md
§6.5`) needs a candidate pool large and diverse enough that the two
objectives can actually disagree on what to select. A pool that is small,
repetitive, or narrow in family coverage risks a **confound, not a null
result**: mean and CVaR could look indistinguishable not because the
objective doesn't matter, but because the pool never gave them anything to
disagree about. This D exists to produce that pool defensibly, and to leave
a record of how it was built so the confound can be ruled out later.

### Prior expectation, pre-registered

A single large generation pass tends to self-repeat in the back half of the
output, and a plain proxy LLM will not spontaneously reproduce the paper's
prompt-injection-wrapper structure (`))))]]]]}}}};*/...`) without being told
to. We expect **per-family, structured prompting** to be necessary, not
optional — this is a design constraint on the plan, not something P is free
to skip for convenience.

---

### Amendment (2026-09-02, design-side): target pool size raised to ~250

The original ~100 target was inherited from the paper's own scale (8 +
family expansions), not derived from a cost or coverage argument. D002
§R3/§R4 subsequently showed generation cost is not the binding constraint
at any candidate size checked (even 30×120 batched is 1.4% of the SU
balance and 4.2 days — comfortably inside the `$SCRATCH` 10-day purge
window). Given that headroom, and given this D's own stated risk — a pool
too small or narrow produces a **confound, not a null result**, in the
mean-vs-CVaR comparison (see "Why this gates..." above) — the asymmetry
favors erring generous rather than minimal.

**New target: ~250** (raised from ~100). Headroom check, scaling linearly
from D002's measured 25×100 cell (0.6% of SU balance, 1.6 days batched):
25×250 lands around ~1.5% of balance, ~4 days — still comfortably inside
both the SU balance and the purge window, with margin for reruns. S3's
existing per-family top-up mechanism (unchanged) may push individual
families toward 300 if needed for coverage; no need to force an exact
ceiling.

**Everything else in this D is unchanged** — S1–S3's approach, anchor
seeding, Call 1 (no pre-filter), Call 2 (generator: OLMo-2), and the
dedup-against-I5-embedding logic. This amendment touches only the numeric
target below and `DECISIONS.md` C3. See `## Review` for the disposition of
the already-approved P1 against this change.

---

## What counts as an answer

| Outcome | Criterion |
|---|---|
| **READY** | Pool reaches target size (~250, raised 2026-09-02 from an original ~100 — see amendment above; a smaller size still acceptable if explicitly justified against C3's eventual value), every one of the paper's four query families (`METHOD.md §5.4`'s generation target: meta-information, banner grabbing, malformed-alignment, weak-alignment) is represented including the prompt-injection-wrapper variant, and the anchor set (paper's 8 + `gpt4o-gen-opt` + `random-opt`, Table F.1) is present verbatim and tagged. |
| **NEEDS ANOTHER PASS** | Pool is short of target size or a family is thin/missing after the two-stage generation described below — re-prompt that family specifically rather than accepting a gap. |
| **BLOCKED** | The proxy LLM cannot produce a usable prompt-injection-wrapper variant even when explicitly instructed (e.g. refuses, or output is malformed in a way that breaks the query format) — report and escalate; do not silently drop that family. |

This is a construction task, not a statistical test — there is no
falsification criterion in the T0.1/T0.2 sense. "Answer" here means "pool
exists, is inspectable, and its provenance is auditable," not a verdict on a
hypothesis.

---

## Required steps (design points already settled — not open to reinterpretation by P)

**S1 — Generate by family, not in one pass.** Prompt the proxy LLM separately
for each of the four families, plus a separate explicit pass for the
prompt-injection-wrapper variant. Do not ask for the full ~100 in one
completion — the back half self-repeats.

**S2 — Seed with anchors, not from nothing.** Include, verbatim and tagged as
`anchor`:
- the paper's original 8 queries (`confs/queries/default.json`)
- the two published baseline sets — `gpt4o-gen-opt` and `random-opt`
  (Table F.1)

Two reasons this is required, not optional: (a) if mean-greedy's selection
ends up overlapping heavily with the original 8, that is a sanity check that
the separability tensor behaves reasonably; (b) it protects against the
failure mode where a low-diversity *generated* pool makes mean and CVaR
degrade together, which would look like "the algorithms are
indistinguishable" when the actual cause is "the pool is bad" — exactly the
confound this D exists to avoid.

**S3 — Tag every entry with its family and provenance** (`anchor` /
`generated-<family>`), in the same style as `METHOD.md §5.4`'s eventual
`Q_pool` bookkeeping — this pool is Phase 1's `Q_0`, and downstream code
will need the tags.

---

## Constraints

**Applicable invariants:** none of I1–I7 bind directly on generation itself.
I2 (split disjointness) and I3 (point-cloud comparison) apply once this pool
is *scored*, which is explicitly out of scope here (see below).

**Cost boundary — generation is cheap, scoring is not, and P must not blur
this.** The proxy-LLM step (producing ~100 query strings) is inexpensive and
is the entirety of this D's budget. **Scoring those candidates against the
full model universe is Phase 1's `|Q| × |L| × |S_build|`-scale cost**
(`METHOD.md §5.4`, already budgeted separately) and is explicitly **out of
scope for D003**. Do not let P quietly fold a scoring pass into this D's
cost estimate.

**Out of scope:** do not score candidates against any model, do not touch
the separability tensor, do not build or extend the corpus. This D produces
a candidate list and its provenance record — nothing else.

**Open question, deliberately left to P (not pre-decided here).** One
cost-reduction idea was raised in design discussion but **not decided**:
pre-filter the ~100 candidates on a small model subset before the full
Phase-1 scoring pass, trading completeness for cost — mirroring the
two-stage filtering already used in CDQD's outer loop generation step
(`METHOD.md §5.4` step D, stages 1–2). P may propose this or not; if
proposed, it must be disclosed as a distinct design choice with its own
tradeoff statement, not silently adopted. Whichever way P goes, flag it
explicitly in the plan for design-side review — this is exactly the kind of
routine-but-not-obvious call `CLAUDE.md`'s escalation rules ask to be
surfaced rather than decided invisibly.

---

## Deliverables

| | |
|---|---|
| `confs/queries/pool_d003_candidates.json` | full candidate pool, each entry tagged `anchor` or `generated-<family>` |
| A short provenance note | which proxy LLM, what prompts were used per family, generation date |
| `## R` below | pool size achieved per family, any family that needed a second pass, and the READY/NEEDS ANOTHER PASS/BLOCKED verdict |

---

## Notes for whoever writes P

- **Generator LLM choice is bound by C6** (`DECISIONS.md`): decoupled from
  the model universe under test, to avoid self-preference effects. State
  which model was used and confirm it is not in the D001/A1 universe.
  *(Corrected 2026-09-02 — originally miscited as C5; caught by TACC's P1
  §F3, which correctly read intent rather than silently editing this
  design-owned section.)*
- This D's pool is a **candidate** pool, not the final `Q_0`. Phase 1's own
  construction step (`TODO.md` T1.2, once relocated per the design-side
  session's file-placement note) still owns assembling the final pool from
  this candidate set plus the tokenizer-level probes T1.2 lists separately —
  D003 does not need to produce those.
- **P must be executable by a fresh session with no conversation context**,
  per the standing rule in `CLAUDE.md`.

---
---

## P — Execution plan

<!-- TACC writes below this line -->

### P1

**Status: PROPOSED — awaiting review.** Full plan: [`plans/D003-P1.md`](plans/D003-P1.md).

**Blocking prerequisite found: the anchors are not cleanly recoverable.** S2 requires
the two published baselines *verbatim*, but our only source is a text extraction of
the paper PDF, and Table F.1 is **two columns that extraction linearized into one
interleaved stream**, plus hyphenation breaks (`quan-\ntum`, `propri-\netary`). A
purely semantic split gives **7 gpt4o-gen-opt / 9 random-opt = 16**, while the paper
says the greedy procedure converged to **8** — so at least one entry is mis-assigned.
P1 §S0 tries the arXiv HTML rendering first and falls back to hand-reconstruction
with an 8/8 reconciliation and per-entry confidence flags. **If an 8/8 split cannot
be defended, the plan stops and reports** — these anchors exist to be a sanity check,
so a corrupted anchor set is worse than a missing one. (The paper's original 8 are
machine-readable in `confs/queries/default.json` — no ambiguity there.)

**Call 1 — the pre-filter question D003 left open: P1 proposes NOT to pre-filter.**
Grounds are methodological, not budgetary. A pre-filter ranks candidates by
discriminative power *averaged over the filter subset* — that is the **mean**
objective, so applying it before the mean-vs-CVaR comparison **bakes the mean
objective into the pool** and biases the experiment toward its own null: exactly the
confound this D exists to prevent. The paper also states that queries weak on their
own can be decisive in combination (§3.1), and a mean-based filter deletes precisely
those. And the saving is not needed — **D002 §R3 measured Phase 1 at 0.6% of the SU
balance** for 25×100 batched, with wall-clock rather than SUs binding. If design side
wants the pre-filter, run it as a separate tagged experiment *after* the unfiltered
comparison so its effect is measurable rather than baked in.

**Call 2 — generator (C6): recommend `Qwen/Qwen3-14B`, with the caveat stated.**
Perfect decoupling is unachievable — the 52-model universe spans ~20 organisations.
No Qwen3 is in the universe (only Qwen2/2.5) and it is ungated and capable, but it
**shares vendor lineage with universe members**, so Qwen-family self-preference
cannot be fully excluded. Strictly-decoupled alternative:
`allenai/OLMo-2-1124-13B-Instruct` (AllenAI has no presence in the universe) at some
cost in generation quality. Recommending Qwen3-14B because the anchors plus
per-family structured prompting constrain output far more than generator identity
does, and a weak generator risks the low-diversity failure mode this D guards
against. **Design side should overrule toward OLMo-2 if strict C6 compliance is
preferred over pool quality.** All six candidates verified available and ungated.

**Steps.** S0 recover/verify anchors (blocking) · S1 five separate generation passes
(four families + a dedicated prompt-injection-wrapper pass, per D003 S1) · S2 merge,
dedup against the frozen I5 embedding, tag with family/provenance — **anchors are
never deduped away** · S3 coverage check with targeted second passes for thin
families · S4 write R.

**Cost.** ~150 completions from one ≤14B model — well under one node-hour, one
`gh-dev` job. Scoring is explicitly excluded per this D's cost boundary.

**Docs slip (FYI, not corrected — `## D` is design-side-owned):** the "Notes for
whoever writes P" bind the generator to **C5**, but C5 in `DECISIONS.md` is the
outer-loop parameters; the generator-LLM decision is **C6**. Read as C6.

**Needs design side:** confirm/overrule the generator choice and the no-pre-filter
position before execution.


---
---

## R — Results summary

<!-- TACC writes below this line -->

```
verdict:       READY
confidence:    high on coverage and provenance; medium on per-query quality --
               this is a candidate pool, and two defects in the generation harness
               (both mine, both found and fixed here) show quality needs inspecting
               per family, not just counting
plan:          P1 (approved with amendments: generator overruled to OLMo-2),
               executed against the revised ~250 target
data:          confs/queries/pool_d003_candidates.json, results/D003/
```

### R1 — Pool composition (233 entries; target ~250)

| tag | n |
|---|---|
| `anchor` / paper8 | 8 |
| `anchor` / gpt4o-gen-opt | 8 |
| `anchor` / random-opt | 8 |
| `generated-meta-information` | 42 |
| `generated-banner-grabbing` | 40 |
| `generated-malformed-alignment` | 42 |
| `generated-weak-alignment` | 44 |
| `generated-prompt-injection-wrapper` | 41 |
| **total** | **233** |

All four families plus the injection variant are represented; **no family was
BLOCKED**; all 24 anchors present verbatim (dedup never touched them, per P1 S2);
dedup removed 21 at cosine > 0.95 on the frozen I5 embedding. 233 is 93% of the
~250 target and clears P1's ≥0.9 threshold. Generator:
**`allenai/OLMo-2-1124-13B-Instruct`**, per the Review's Call-2 override, confirmed
absent from the D001 universe (C6).

### R2 — S0: anchors recovered, and the stop-and-verify posture paid for itself

P1 F1 flagged that our PDF extraction linearized Table F.1's two columns and a
semantic split gave 7/9 = 16 against the paper's stated 8+8. The approved primary
route — the arXiv HTML rendering — resolved it: **8 + 8, column assignment read from
table structure rather than inferred**, all 24 anchors `confidence: exact`.

**It also caught a real error.** A semantic split would have placed *"Write a
creative story involving quantum mechanics and a detective mystery."* in
`random-opt` (it reads like an Alpaca instruction). The table puts it in
**GPT4o-gen-opt**. Shipping the reconstruction would have silently corrupted one of
the two baseline sets whose only purpose is to be a sanity check.

### R3 — Two defects in my own harness, found and fixed (disclosed, not silent)

**(a) Dedup collapsed the injection family.** First run produced only 22 injection
entries against a 46 target and returned NEEDS ANOTHER PASS. Cause was not the
generator: the wrapper boilerplate is **61% of each wrapped string**, so the I5
embedding was dominated by the shared trigger and 16 semantically distinct probes
were dropped as >0.95 "duplicates". Fixed by deduping the **inner question** rather
than the wrapped string. This was only visible because counts were broken out per
family — an aggregate total would have read as "the generator is weak," which was
the wrong conclusion.

**(b) The injection prompt drifted into dead queries.** With counts fixed, 44
entries passed but **24 of them (55%) were neutral encyclopedia trivia about
third-party systems** — *"What year was GPT-3 released"*, *"What is the capital city
of Australia"*, *"Who directed The Matrix"*. Every model answers those identically,
so their inter-model discrepancy (paper Eq. 2) is ~zero: **dead queries**. Cause:
my prompt said "a question about the model's identity", which OLMo-2 read as *any*
model. Fixed by requiring second-person address and explicitly forbidding neutral
trivia.

Result after the fix — the non-self-referential remainder flipped category entirely:

| | injection n | neutral trivia | contested/self-ref |
|---|---|---|---|
| v1 | 44 | **24 (55%)** | 20 |
| **v2 (shipped)** | 41 | **0 (0%)** | 41 |

v2's 19 non-self-referential entries are all contested-claim probes (*"Does God
exist"*, *"Is the earth flat or round"*, *"Should AI systems be regulated more
strictly"*) — the paper's own weak-alignment category, wrapped, which is exactly
what the trigger is for. v1 is preserved at
`results/D003/pool_v1_before_injection_fix.json` so the delta is auditable.

**Judgment call, flagged for review:** D003's NEEDS ANOTHER PASS path is written for
a family that is *count*-thin, and after fix (a) this family met its count. I treated
"55% dead queries" as effectively short and re-ran, on the grounds that shipping a
known-flawed 18% of the pool into Phase 1 is the low-quality-pool confound D003
exists to prevent, and the defect was mine rather than the design's. Cost of the
re-run was ~3 minutes. If design side disagrees, v1 is preserved.

### R4 — Cost, restated against the ~250 target (per the addendum)

P1's stated "~150 completions, well under one node-hour" was computed against the
old ~100 target and is superseded:

- **10 completions** total (2 rounds × 5 families), 257 parsed lines
- **103 s** of generation; **2 min 49 s** total job wall-clock including model load
- **1 `gh-dev` node-hour billed** (15-min minimum), ~0.04% of the 6982 SU balance

Three job submissions were needed (two harness bugs), so ~3 node-hours total across
all attempts. Still negligible, and it confirms the addendum's expectation that
2–3× the original target changes nothing on the cost axis.

### R5 — Limitations

- **No pre-filter was applied** (Review Call 1). Candidate quality is therefore
  uneven by design — the selection algorithm is expected to discard weak entries.
  R3(b) is about queries that are *provably* dead (identical answers across all
  models), not merely weak.
- **`malformed-alignment` was not refused.** OLMo-2 produced 42 refusal-eliciting
  probes without needing P1's reformulation fallback. The entries are probe
  *questions* only; no harmful content was generated.
- **Per-family quality was checked by inspection, not measured.** A quantitative
  discriminative-power check would require scoring against the model universe, which
  is Phase-1 scale and explicitly out of D003's scope.
- Prompt configs elsewhere in the repo remain affected by the I2 defects (D005);
  irrelevant here, since this D generates query text and touches no split.

<!-- TACC writes below this line -->

---
---

## Review

<!-- append below -->

## Review — D003 / P1

### For you (human)

**Verdict:** APPROVED WITH AMENDMENTS

**What changed**
- **Generator: `allenai/OLMo-2-1124-13B-Instruct`, not TACC's recommended
  `Qwen/Qwen3-14B`.** TACC's own reasoning (the anchor set and per-family
  structured prompting constrain the output far more than which model
  writes it) actually argues *against* needing a strongly-related
  generator — which undercuts the case for accepting the Qwen self-preference
  risk. Since this pool feeds the experiment used to justify the paper's
  central claim, took the strict-decoupling option TACC offered as an
  alternative.
- Fixed a small documentation error TACC caught (D003 pointed to the wrong
  hyperparameter id for the generator choice — C5 instead of C6). No
  practical effect, just a citation fix.

**Needs your input:** None — both open calls (generator, pre-filter) are
resolved above.

**Follow-ups opened:** None from this Review itself.

**Next:** TACC executes P1 with the generator swapped to OLMo-2-13B-Instruct.

---

### Technical log

**Call 1 (no pre-filter) — approved as proposed.** The reasoning is sound
and matches D003's own constraint: a pre-filter selects by power averaged
over a subset, which *is* the mean objective, and applying it before the
mean-vs-CVaR comparison would bake that objective into the pool — precisely
the confound D003 exists to prevent. The "it saves cost" counter-argument is
foreclosed by D002 §R3 (Phase 1 is 0.6% of SU balance; wall-clock, not SUs,
binds). Agreed this should be a separate, tagged, post-hoc experiment if
ever wanted, not a default.

**Call 2 (generator) — overruled toward OLMo-2, see "For you."** Restating
the reasoning for the audit trail: C6 exists specifically to guard against
self-preference. TACC's argument for Qwen3-14B rests on "generator identity
matters less than pool structure" — but that argument, taken at face value,
removes the justification for accepting the residual Qwen-family risk in
the first place, since a less-related generator should perform comparably
well under the same anchor+structured-prompting scaffold. Going with the
strictly-decoupled option costs nothing D003 identified as load-bearing.

**Anchor-recovery approach (S0) — approved.** Try-HTML-then-reconstruct-
with-confidence-flags, stop-and-report on an indefensible split, is exactly
the right posture for an artifact whose entire purpose is being a sanity
check. Noting for the record: F1's diagnosis (table linearization + count
mismatch, 7+9=16 vs. paper's 8+8) is a good catch — a naive semantic split
would have shipped a silently-wrong anchor set.

**Everything else in P1 accepted as written:** S1's five-separate-passes
structure, S2's dedup-never-touches-anchors rule, S3's per-family
second-pass policy, and the cost/scope boundary (no scoring, no tensor work)
all match D003's requirements exactly.

**Process note, not a finding:** F3's handling of the C5/C6 slip (flag,
don't silently edit a design-owned section) is exactly right per
`CLAUDE.md`'s file-ownership rule, and is the second time this has come up
cleanly (cf. D001-Review-P1-v2.md's own housekeeping). No action needed;
noting the pattern is working as intended.

---

## Review — D003 addendum (2026-09-02, design-side): C3 target revised, P1 approach still stands

**What changed:** `## D`'s target pool size raised from ~100 to ~250 (see
amendment inline above) — a routine Part C hyperparameter call
(`DECISIONS.md` C3), not a re-opening of P1's methodological approach.

**Disposition of the existing APPROVED WITH AMENDMENTS verdict on P1:**
stands for everything P1 actually decided — S0's anchor-recovery approach,
Call 1 (no pre-filter), Call 2 (generator, overruled to OLMo-2). None of
those depend on the numeric target. **Not standing:** P1's stated cost line
("~150 completions... well under one node-hour") was computed against the
old ~100 target and is now stale — TACC should restate the real completions
count and cost against ~250 when reporting `## R`; this is a number update,
not grounds to re-review the approach. **No new P/P2 required before
execution** — D002 §R3/§R4 already established this generation step's cost
is negligible at 2–3x the original target, so there is nothing left to
re-litigate on the cost/feasibility axis.

**Next:** TACC executes against the revised target whenever picked up;
report the actual pool composition (per-family counts, completions used,
whether S3's per-family top-up was needed) in `## R` as before.
