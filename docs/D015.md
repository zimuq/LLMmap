# D015 — Per-pair decomposition of `hard_subset`: is the gain spread across the 65 structural pairs, concentrated, or does any pair get worse?

```
id:          D015
source:      docs/REVIEW-2026-09-22-hard-pairs.md §8, §10.A (retrospective
             audit requested by the human, 2026-09-22): `hard_subset` is
             a single number averaged over 65 pairs; it is neither the
             worst pair nor a tail average, and per-pair behaviour has
             never been reported beyond two named pairs (Falcon3-10B/7B,
             Phi-3-medium-128k/4k) shown descriptively in that review.
             Human-approved 2026-09-22 ("2的提案非常合理,可以立刻写D").
status:      OPEN
gate:        REVIEW          # produces numbers a writeup could use;
             default per CLAUDE.md. Cost is trivial (no training), but
             the framing (what counts as "concentrated" vs "spread", how
             per-pair CIs are computed) should be checked before it's
             taken at face value, per this project's standing practice.
deps:        D007 (frozen tensor, for pair↔index mapping only), D009
             (paper8, cvar_max=coverage γ=0.1 trained runs, k=1..8),
             D010 (joint_energy trained runs, k=1..8), D012 (JointGreedy
             γ∈{1.0,0.25,0.1,0.05} trained runs), D013 (GreedyCover
             γ∈{1.0,0.5,0.25,0.1,0.05} trained runs) — all CLOSED. No
             dependency is rerun; this D reads their saved
             `per_hard_pair` arrays only (I6).
opened:      2026-09-22
closed:      —
downstream:  Directly informs whether the two previously-named hard
             pairs are representative of a broader pattern or outliers;
             a prerequisite for ever defining a frozen empirical hard set
             `H_ref` (not attempted here — see Constraints), which would
             itself be a later, separate, human-approved decision.
```

---

## D — The question

> **Break the single `hard_subset` number (mean two-logit restricted
> accuracy over the 65 structural near-relative pairs, `METHOD.md §8`)
> into its 65 pair-level components, for `paper8`, `coverage`
> (`cvar_max`, γ=0.1), and `joint energy`, at matched `k`. Does the
> ≈0.4–0.5pp aggregate gain (D010, k=8) come from most pairs improving a
> little, a few pairs improving a lot while the rest are flat, or some
> pairs improving while others get worse (redistribution the aggregate
> would hide)?**

### What is and is not known going in

- The aggregate `hard_subset` numbers are already reported (D010, k=8:
  paper8 .9637, coverage .9644, joint energy .9685) and are small in
  absolute terms — the entire random-to-oracle range on this metric is
  only ~7.5 points (D008's own finding, the reason D008 was INCONCLUSIVE
  by the letter).
- Every run in D009/D010/D012/D013 already stores a `per_hard_pair` list
  of 65 floats per run (verified present, 2026-09-22). No new training,
  selection, or generation is needed — this is a read-and-reorganize job.
- The two previously-named difficult pairs (Falcon3-10B/7B,
  Phi-3-medium-128k/4k) are **not** derivation pairs — checked against
  `results/D001/model_metadata.csv`: both have an empty `base` field
  (no declared fine-tune parent/child relationship between the two
  models in the pair). They match on `lineage` only (`falcon-3`,
  `phi-3` respectively) — i.e. they are siblings released in the same
  model family/generation, not one derived from the other. Of the full
  65 structural pairs (37-model universe), 6 also share a literal `base`
  field (true fine-tune-of-shared-parent pairs), and those 6 are a
  subset of the 65 `same_lineage` pairs, not a disjoint category, in
  this dataset.
- What is *not* known: whether the aggregate gain is broad or narrow: no
  per-pair breakdown beyond the two named pairs has been reported.
- **Added 2026-09-22, per the human's question about where "hard" comes
  from for the two named pairs.** Their difficulty label has three
  independent sources, of different strength, none of them yet a
  trained/frozen/systematic protocol: (1) **D001** — an unsupervised
  open-set nearest-template AUC statistic computed on the *paper's own
  shipped artifact* (`default/` pretrained templates over 52 models),
  before this project generated any corpus of its own; Falcon3-7B↔10B
  was "the hardest pair in the entire matrix" there, Phi-3-medium-4k↔128k
  the 4th-hardest. External to our pipeline, but not a trained
  classifier. (2) **D008** — a single-query proxy *oracle* (best-of-pool,
  not a selected chain) on our own frozen `S_energy` tensor/corpus:
  0.840 / 0.800. Same corpus D009–D014 use, so not independent of it,
  and the review's 2026-09-22 audit (§4) already flagged this oracle
  number as a different estimand from a trained k-query classifier — not
  a ceiling on it. (3) **D010's saved per-seed trained numbers**
  (coverage .656–.800, joint energy .732–.832) — real trained accuracy,
  but never assembled into a clean per-pair report until this D. No
  source here is a prospectively-frozen, independent-development-set,
  trained-classifier measurement — see `H_ref` in Constraints.

---

## What counts as an answer

Report, per pair, per method, at `k`=8 (the project's standard headline
budget) and `k`=1 (smallest budget, for contrast):

- mean accuracy across the already-run 5 seeds, with a bootstrap CI
  computed the same way the project already computes per-cell CIs
  (paired at the config-bootstrap level, per D009's convention) —
  **note going in that these will be noisier than the aggregate**: each
  pair's number comes from ~50 evaluation traces (2 models × 25 test
  configs) × 5 seeds, an order of magnitude less data than the 65-pair
  average. State this caveat in `R`, don't let a narrow per-pair CI be
  read as a null result.
- Δ(joint energy − coverage) and Δ(coverage − paper8) per pair, flagged
  by sign — **explicitly list any pair where Δ is negative** (joint
  energy or coverage does worse than the comparator), not just the
  positive ones.
- Where the two previously-named pairs rank in this list — are they the
  hardest under every method (consistent with "structurally hard
  regardless of selection"), or does their rank change by method
  (consistent with selection-algorithm-dependent difficulty)?

**Not required:** a hardness ranking presented as an adopted definition
(`H_ref`) — see Constraints. **Not required:** worst-class per-pair
analysis, or any new `γ`/`k` combination beyond what D009/D010/D012/D013
already ran.

**Also required, for presentation use (added 2026-09-22):** the full
65-row table is the audit artifact, not the thing to hand to a
supervisor. Produce, alongside it, three short derived views (S6) —
a plottable γ×`k` curve for the two named pairs, a hardest-8 table, and
a per-family rollup — each under ~10 rows, each clearly labelled as a
*subset or aggregation of* the full table, not a separate measurement.

---

## Required steps

**S0 — Confirm consistent pair indexing across the four source files.**
Each of D009/D010/D012/D013's runs was produced against the same frozen
37-model universe (I6), so `near_relative_pairs(models)`'s 65-entry
index→(model_a, model_b) mapping should be identical across all of
them. Verify this explicitly (e.g. assert the same `models` list, or
the same derived pair-name list, in each file's metadata) before joining
any numbers across files — a silent reordering would silently corrupt
every downstream row. Report the check's outcome in `R` even if it
merely confirms the expected.

**S1 — Core deliverable: the 65×3 table at `k`=8.** For each of the 65
pairs, report `paper8`, `coverage` (γ=0.1), `joint energy` mean accuracy
(5-seed mean + CI) and the two deltas above. Source: D009's `paper8` and
`cvar_max` runs at `k`=8, D010's `joint_energy` runs at `k`=8. Sort for
readability by `paper8`'s accuracy (hardest-under-paper8 first) —
label this sort as a display convenience, not a hardness claim.

**S2 — Contrast table at `k`=1.** Same construction, `k`=1, to see
whether concentration/spread looks different at the smallest budget.

**S3 — Required: the two named pairs' full γ×`k` curve.** D012
(JointGreedy, γ∈{1.0,0.25,0.1,0.05}) and D013 (GreedyCover,
γ∈{1.0,0.5,0.25,0.1,0.05}) already have `per_hard_pair` stored for every
γ×`k` cell they ran (no new computation — pure extraction + the same
per-cell CI procedure as S1). For **Falcon3-10B/7B** and
**Phi-3-medium-128k/4k** only, report accuracy at every stored (γ, `k`)
point, both algorithms, in a shape ready to plot as a line chart
(x=`k`, one line per γ, one panel per algorithm per pair — or
equivalently a long-format table with columns `pair, algorithm, gamma,
k, mean, ci_lo, ci_hi`). **Do not build a denser pair×γ×`k` grid than
what D012/D013 already ran** — this reports exactly those stored cells,
nothing interpolated or newly computed; label every cell with which
source D it came from.

**S4 — Explicitly separate three things in `R`, per the review's
typology (do not conflate them):** (a) this D's 65-pair table is the
existing structural set `N` (metadata-defined, fixed since D001/D008
specifically to avoid scoring a selection algorithm against a hardness
definition drawn from its own tensor) — not a hardness-ranked
benchmark; (b) the two previously-named pairs, shown in context among
the other 63, not cherry-picked; (c) note explicitly that this D does
**not** construct a frozen empirical hard set (`H_ref`) — that requires
a reference protocol, an independent development split, and a
pre-registered threshold, none of which exist yet, and adopting one as
a primary endpoint is a separate human decision.

**S5 — Report descriptively; reuse existing statistics code.** No new
significance-testing framework — the same bootstrap/CI procedure already
used for aggregate metrics in D009–D014, applied per pair instead of
averaged over pairs. Do not invent a new test.

**S6 — Presentation-ready summary views, each derived from S1–S3's data,
not separately measured.** All under ~10 rows:

- **Hardest-8 table** — the 8 pairs with the lowest `paper8` accuracy at
  `k`=8 (from S1), all three methods, same columns as the full table.
  Confirm where the two named pairs land in this list (should be at or
  near the top, given they were flagged hardest by D001/D008 — report
  the actual rank, do not assume it).
- **Family rollup** — group the 65 pairs by `lineage` tag (e.g. all
  `llama-3`-tagged pairs, all `qwen2`-tagged pairs, …) and report each
  family's mean accuracy per method (mean of its member pairs' S1
  numbers, unweighted within family) plus member count. State the
  weighting explicitly (larger families get one row, same as small
  ones) — this is a display choice, not a claim that families should be
  weighted equally in `hard_subset` itself.
- **Moved-backward table** — every pair (if any) where
  Δ(joint energy − coverage) or Δ(coverage − paper8) is negative with
  the CI excluding zero (S1's flagged list, filtered to only the
  resolved-negative rows). Expected to be short or empty; report
  "none" explicitly if so, rather than omitting the table.
- **S3's two-pair γ×`k` curve** already satisfies the "presentation
  view" bar on its own (≤5 γ values × 8 `k` values per pair per
  algorithm) — no further compression needed there.

---

## Constraints

**Applicable invariants:** I6 (every number sourced from
D009/D010/D012/D013's already-frozen runs; no re-selection, no
re-training, no re-generation, no re-embedding).

**Out of scope:** constructing `H_ref` (S4c); any new `γ` or `k` value;
worst-class per-pair analysis; any claim that a pair is "solved" or
"intrinsically hard" from these numbers alone (per-pair CIs will be wide
— report them, do not round them away); editing `METHOD.md`.

**Cost:** ~0 GPU time — a data-processing script over already-saved
JSON, plus bootstrap CIs (CPU, minutes at most). **State the measured
wall time in `R`** anyway, per standing project practice of always
reporting it, however small.

---

## Deliverables

| | |
|---|---|
| `results/D015/per_pair_k8.json` | S1: 65-row table, 3 methods, means + CIs + deltas |
| `results/D015/per_pair_k1.json` | S2: same, `k`=1 |
| `results/D015/per_pair_gamma_named.json` | S3: the 2 named pairs across the full stored γ×`k` grid, both algorithms, plot-ready long format |
| `results/D015/summary_hardest8.json` | S6: hardest-8 table |
| `results/D015/summary_family_rollup.json` | S6: per-lineage rollup |
| `results/D015/summary_moved_backward.json` | S6: any resolved-negative pairs (or an explicit empty result) |
| `## R` below | the rendered tables (full + S6 summaries), the S0 indexing check, and the S4 typology separation |

---

## Notes for whoever writes P

- **This is the cheapest D in the project by GPU cost (zero) — the risk
  here is entirely in bookkeeping (S0's indexing check) and in framing
  (S4), not in compute.** Spend the plan's care there, not on run
  efficiency.
- **Report every pair, including the boring ones, in the full S1/S2
  tables.** A table that only shows pairs where something moved would
  defeat the point (that is exactly the "hard_subset hides
  redistribution" concern this D exists to check). **S6's summaries are
  additional, not a replacement** — they exist because the human wants a
  short version for a supervisor presentation; keep the full 65-row
  data too.
- **If S0's indexing check fails** (pair order differs across source
  files), stop and report it in `R` rather than guessing a fix — that
  would be a data-integrity issue in prior D's, not something to patch
  silently inside this one.
- **P must be executable by a fresh session with no conversation
  context**, per the standing rule in `CLAUDE.md`.

---
---

## P — Execution plan

<!-- TACC writes below this line -->

### P1

**Status: PROPOSED — awaiting review.** Full plan:
[`plans/D015-P1.md`](plans/D015-P1.md). S0's indexing check has been run
(read-only, seconds); S1–S6 have not. **No existing repo file modified.**

D015 says the risk is bookkeeping and framing, not compute. Correct — and S0 found
three things worth settling before any table exists. One would have left the core
deliverable partly empty; one sets how hard the tables can be read.

**S0 — passes, with a caveat about how.** `results/D009/runs.json` stores the
37-model list; **D010, D012 and D013 do not**, so literal list-equality across all
four is not available from the artifacts. What is provable: all four derive the
index identically — `near_relative_pairs(models)` in `d008_lib`, `models` from
`results/D008/selection.json`, `sorted(hard_pairs)` as row order — and every
source's `cnt_pair` is **(65, 25)** with `per_hard_pair` of length **65**.
**Proposal: report this as verified by construction plus shape, not by stored
metadata**, plus F4's value-level corroboration, rather than claiming a stronger
check than the files support. All four sources have `per_hard_pair` on **100% of
non-error runs** (D009 160, D010 45, D012 120, D013 120).

**F1 — S3 cannot be built from the files it names, and D014 fixes it.** D012 and
D013 store runs only for the γ they *trained*; the γ they **reused under I6** have
no runs of their own. JointGreedy γ=0.1 lives in **D010**; GreedyCover γ=1.0 and
γ=0.1 live in **D009**. This is the same reuse-map trap that blanked a cell in
D014's first pass — named here so it is handled before the table, not after.

**Call 1 — include D014's cells in S3?** D014 closed 2026-09-19, *after* D015 was
drafted, and is not in its `deps` — but it holds `per_hard_pair` for **520 runs
across 13 more (algorithm, γ) cells** (GreedyCover γ ∈ {0.75, 0.6, 0.45, 0.4,
0.35, 0.3}; JointGreedy γ ∈ {0.75, 0.6, 0.5, 0.45, 0.4, 0.35, 0.3}). Including
them makes S3's curve **11 γ values per algorithm instead of 4–5, at zero
compute.** D015 says *"do not build a denser grid … nothing interpolated or newly
computed"* — I read that as a ban on **computing** new cells, which this is not,
but D014 is outside the stated deps so I am asking. **Proposal: include, labelled
by source D per cell as S3 already requires** — D014's own conclusion (γ is
continuous but strongly non-linear) is exactly what a γ-curve should show, and
showing it over 4 points when 11 exist would misrepresent the shape.

**F2 — use the per-pair config bootstrap, not a 5-seed one.** Every run stores
`cnt_pair` (65, 25): per pair, correct traces per test config. Proposal: one set
of 2,000 `boot_draws` multiplicity vectors shared across all methods and both `k`,
so every Δ is **paired on identical draws** — exactly the aggregate convention of
D009–D014, applied per pair. No new test (D015/S5).

**F3 — the resolution floor, and it should precede the tables in `R`.**

| | |
|---|---|
| effect being decomposed (joint − coverage, `k`=8) | **+0.41 pp** |
| `hard_subset`'s evidence base | 65 × 2 × 25 = **3,250 trace decisions** |
| what +0.41 pp is | **≈13 trace flips total** — 0.21 per pair |
| smallest step one pair can show | 1 trace of 50 = **2.00 pp** |

**The quantity being decomposed is an order of magnitude smaller than the
granularity of a single pair.** No pair *can* move by less than 2 pp, so "most
pairs improving a little" is arithmetically impossible to observe; whatever
appears will be a few pairs at ±2 pp and the rest at exactly zero — a pattern
forced by the denominator, not evidence about concentration.

**This sharpens the D rather than voiding it.** *"Does any pair get worse"* —
redistribution the aggregate hides — **is** fully answerable and is the half that
matters. *"Spread vs concentrated"* is **not answerable at 25 test configs**, and
`R` will say so rather than report the forced pattern as a finding. Same wall
D008/F3, D009, D010/R9 and D011 hit; it binds hardest here because the denominator
is per pair.

**F4 — a value-level corroboration, since metadata cannot supply one.** Report
Falcon3-10B↔7B's **rank among the 65 independently in each of the four sources**.
D001 (external, pre-corpus) and D008 (tensor oracle) both flag it hardest or
near-hardest; a permuted index would make its rank jump between files. Agreement
is corroboration, not proof, and `R` will label it so.

**Cost:** zero GPU, minutes of CPU. **New file only:**
`experiments/d015_per_pair.py`.

**Sequencing:** only Call 1 gates anything (S3's scope). S1, S2, S4–S6 proceed
regardless.

---
---

## Review — D015 / P1

### For you (human)

**Verdict:** APPROVED — Call 1 and all four findings (F1–F4) accepted as proposed.
No amendments; TACC may execute S1–S6 directly.

**The one thing to know before the tables land:** TACC found (F3) that the
aggregate effect this D is decomposing (+0.41pp) is worth only about **13
individual trace-flips total across all 65 pairs** — so **no single pair can
move by less than 2 percentage points**, an order of magnitude coarser than
the effect itself. That means the table **cannot** show "most of the 65 pairs
improved a little" as a real pattern — whatever shows up will just be a
handful of pairs at ±2pp and the rest at exactly zero, forced by arithmetic,
not a finding. What the table **can** still answer, and will: whether any
pair moved *backward* (a real redistribution check), and where the two named
pairs rank across all four independent data sources. Good catch — this is
exactly the kind of resolution honesty this project has valued since D008.
**For the supervisor deck: lead with "does any pair get worse" and the named
pairs' cross-source ranking, not a spread-vs-concentrated claim.**

**Needs your input:** None.

**Follow-ups opened:** None.

**Next:** TACC executes S1–S6. Per your instruction this session — flagged to
TACC in the technical log below: this is 0 GPU / minutes of CPU, so it should
run directly on a login node, not queued through SLURM.

---

### Technical log (for TACC / audit trail)

**Call 1 — approved.** Include D014's 13 extra (algorithm, γ) cells in S3,
labelled by source D per cell. D014 postdating D015's stated `deps` is a
drafting-order artifact, not a scope restriction — D015/S3 asks for "every
stored (γ, k) point" for these two named pairs, and D014's cells are exactly
that: already-frozen, already-trained, zero new compute, same I6 corpus. The
"do not build a denser grid" line in D015 was written to forbid computing new
cells, not to cap which already-existing cells get read; TACC's own reading
of it is correct. Not using an 11-point curve when it already exists for free
would be the worse outcome for the presentation use case this D exists to serve.

**F1 — accepted.** The reused-γ storage gap (JointGreedy γ=0.1 → D010;
GreedyCover γ=1.0/0.1 → D009) is exactly the D014-first-pass trap correctly
generalized here. Good that it was caught before S3 ran rather than after.

**F2 — accepted.** Per-pair config bootstrap from `cnt_pair` (65, 25), shared
2,000-draw multiplicity vectors across methods/k, is the correct generalization
of D009–D014's own paired-bootstrap convention — not a new statistical
framework, exactly as D015/S5 required.

**F3 — accepted, and this is the substantive finding of P1.** The arithmetic
is right and the proposed scoping is the honest response to it: report the
resolution floor *before* the tables in `R`, answer "does any pair get worse"
(answerable, and the half that matters for the redistribution question D015
was drafted to check), and explicitly label "spread vs concentrated" as
unanswerable at 25 test configs rather than reporting the forced pattern as
if it were a result. This is the same discipline that kept D008 from
over-claiming on `hard_subset` in the first place (its 7.5-point total range)
— consistent, not a new standard being invented for this D.

**F4 — accepted.** Cross-source rank agreement (D001 external pre-corpus AUC,
D008 tensor oracle) is a reasonable value-level substitute for the model-list
equality check three of the four source files don't store, correctly labelled
as corroboration rather than proof.

**Execution note, per the human this session — not a change to S0–S6, an
operational instruction:** `experiments/d015_per_pair.py` is 0 GPU time and
minutes of CPU (P1's own cost line). Run it directly on a fast login node,
not through a SLURM queue — there is nothing here that needs a scheduled job
or GPU allocation, and queuing it would only add wait time for no benefit.
If TACC's environment has a reason this specific script still needs to go
through the batch queue (e.g. no compute access at all from login nodes),
say so in `R` rather than silently queuing it.

**No amendments to S1–S6 as written in P1.** Proceed as planned.

---

## R — Results summary

<!-- TACC writes below this line -->

# **The aggregate hides large, offsetting per-pair movement. 20 of 65 pairs move backward; 3 of them resolvably.**

D015's three-way question resolves cleanly into one answered half and one
unanswerable half, exactly as P1/F3 predicted before the tables existed.

**`joint energy` vs `coverage` at `k`=8 — the +0.41 pp aggregate, decomposed:**

| | |
|---|---|
| pairs that improve | **28** |
| pairs that get **worse** | **20** |
| pairs exactly flat | 17 |
| sum of all positive moves | **+0.544** |
| sum of all negative moves | **−0.276** |
| net | +0.268 → **÷65 = +0.0041**, the reported aggregate |

**The headline number is a 0.544 gross gain and a 0.276 gross loss reported as a
0.0041 net.** Half the movement cancels. `coverage` vs `paper8` is the same shape:
28 up, 21 down, 16 flat, netting +0.0007.

**This is the redistribution D015 existed to check for, and it is real.**

### R1 — What the table cannot show, and why (P1/F3, confirmed in the data)

The observed per-pair move sizes are **0.000, 0.004, 0.008, 0.012, 0.016, 0.020,
0.024, 0.032** — all multiples of 0.004 = 1 trace in 250 (50 traces × 5 seeds).
No intermediate value exists or can. So "most pairs improved a little" is not a
pattern the instrument can render, and the actual distribution — 28/20/17 with a
gross-to-net ratio of 3:1 — is what a small net effect over a coarse grid looks
like.

**The spread-vs-concentrated branch of D015's question is therefore reported as
unanswerable at 25 test configs**, per P1/F3 as approved. The redistribution
branch is answered, and it is the one that matters: **an aggregate `hard_subset`
gain of +0.41 pp is consistent with a third of the hard pairs getting worse.**

### R2 — Resolved-negative pairs: 15 rows, and the two hardest pairs are in them

15 rows have Δ < 0 with the bootstrap CI excluding zero (3 at `k`=8 for
joint−coverage, 3 at `k`=8 for coverage−paper8, 9 at `k`=1 for coverage−paper8):

| `k` | pair | contrast | Δ | CI |
|---|---|---|---|---|
| 8 | **Phi-3-medium-128k ↔ 4k** | coverage − paper8 | **−0.116** | [−0.212, −0.024] |
| 8 | Phi-3-medium-4k ↔ Phi-3-mini-128k | coverage − paper8 | −0.112 | [−0.196, −0.032] |
| 8 | gemma-1.1-2b ↔ gemma-1.1-7b | coverage − paper8 | −0.072 | [−0.128, −0.024] |
| 8 | Llama-3.2-3B ↔ Meta-Llama-3-8B | joint − coverage | −0.040 | [−0.088, −0.004] |
| 8 | Meta-Llama-3-8B ↔ 3.1-8B | joint − coverage | −0.032 | [−0.064, −0.004] |
| 8 | Mistral-v0.3 ↔ openchat_3.5 | joint − coverage | −0.020 | [−0.036, −0.008] |

**The single hardest pair in the set — `Phi-3-medium-128k ↔ 4k` — is 11.6 points
worse under `coverage` than under the paper's 8 queries**, resolvably, at the
headline budget. `joint energy` recovers it (0.732 vs coverage's 0.656) but only
back to roughly paper8's 0.712. That is a specific, citable cost of the selection
methods on the specific pair this project has named as hardest since D001.

### R3 — F4's corroboration: the two named pairs rank 1 and 2 of 65, under **every** method

| | rank by `paper8` | by `coverage` | by `joint energy` |
|---|---|---|---|
| **Phi-3-medium-128k ↔ 4k** | **1** | **1** | **1** |
| **Falcon3-10B ↔ 7B** | **2** | **2** | **2** |

Identical ranking under three independently-selected query sets, and consistent
with two sources outside this corpus entirely (D001's template AUC on the paper's
own shipped artifact; D008's tensor oracle). **The pairs are hard regardless of
selection algorithm** — not an artifact of one method's chain. It also corroborates
S0's index join: a permuted index would not place the same two pairs first in every
source.

**They are not fine-tune derivation pairs.** Both have an empty `base` field —
siblings in one release family, not parent/child. Of the 65, **6** share a literal
`base`, and none of those 6 is in the hardest 8. Whatever makes these two hard, it
is not a declared derivation relationship.

### R4 — S0: the join is sound, verified the way the artifacts allow

`cnt_pair` is **(65, 25)** in all five sources (D009 160 arrays, D010 45, D012 120,
D013 120, D014 520), and all five derive their index identically —
`near_relative_pairs(models)` over `results/D008/selection.json`'s model list,
row order `sorted(hard_pairs)`. **Only D009 stores a model list**, so this is
verified by construction plus shape, not by stored metadata; R3's rank agreement is
the value-level corroboration standing in for the check the files cannot supply.
Reported as P1 proposed rather than claimed as stronger than it is.

### R5 — S2 at `k`=1 is structurally degenerate for one of its two contrasts

**All 65 joint−coverage deltas at `k`=1 are exactly zero.** Not a null result — at
`k`=1 the joint statistic reduces to the per-query one (D010/R2, verified to the
bit), so `coverage` and `joint energy` select the *same single query* and are the
same condition. The `k`=1 contrast is informative for coverage−paper8 (9 resolved
negatives, all pairs worse under coverage) and **carries no information at all**
about joint vs coverage. Flagged because D015/S2 asked for `k`=1 "to see whether
concentration looks different at the smallest budget" — for that contrast the
question is void by construction, not unanswered.

### R6 — S3: the named pairs' γ curve, now 11 γ values per algorithm (Call 1)

Including D014's cells (approved) gives γ ∈ {1.0, 0.75, 0.6, 0.5, 0.45, 0.4, 0.35,
0.3, 0.25, 0.1, 0.05} for **both** algorithms, 352 plot-ready rows. `Falcon3-10B ↔
7B` under JointGreedy at `k`=8:

| γ | 1.0 | 0.75 | 0.6 | 0.5 | **0.45** | 0.4 | 0.35 | 0.3 | 0.25 | 0.1 | 0.05 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| acc | 0.684 | 0.752 | 0.692 | 0.732 | **0.824** | 0.828 | 0.804 | 0.832 | 0.832 | 0.832 | 0.832 |

**This single pair reproduces D014's RAMP at pair level**: flat-and-noisy above
γ≈0.5, a step up at 0.45, then a plateau at 0.832 from γ≤0.3 down. Had S3 been
built from D012/D013's 4–5 γ values only, the 0.45–0.3 region — where the whole
transition happens — would have been a single gap between 0.5 and 0.25. **Call 1
was load-bearing, not cosmetic.** (Per-pair CIs here are ±0.07, so this is one
pair's illustration of a pattern D014 established on aggregates, not independent
evidence for it.)

### R7 — Family rollup (display choice: one row per lineage, unweighted)

| lineage | n | paper8 | coverage | joint energy |
|---|---|---|---|---|
| llama-3 | 21 | 0.9863 | 0.9905 | 0.9878 |
| mistral-7b | 15 | 0.9515 | 0.9672 | 0.9635 |
| **phi-3** | 10 | 0.9216 | **0.8964** | 0.9256 |
| gemma | 10 | 0.9928 | 0.9824 | 0.9880 |
| qwen2 | 6 | 0.9660 | 0.9753 | 0.9727 |
| falcon-3 | 1 | 0.8040 | 0.8000 | 0.8320 |
| granite-3 | 1 | 0.9480 | 0.9560 | 0.9600 |
| llama-2 | 1 | 0.9640 | 0.9800 | 0.9920 |

**`phi-3` and `gemma` are the two families where `coverage` is worse than
`paper8`**, and phi-3 is where the resolved negatives concentrate. Equal weight per
family is a display choice, not a claim about how `hard_subset` should weight them.

### R8 — What this D did **not** do (S4's typology, kept separate)

- **(a)** These 65 are the **structural set `N`** — metadata-defined since
  D001/D008 precisely so that a selection algorithm is never scored against a
  hardness definition drawn from its own tensor. The `paper8`-ascending sort is a
  display convenience. **This is not a hardness benchmark.**
- **(b)** The two named pairs are shown **in context among the other 63** (full
  65-row tables at both `k`), not cherry-picked.
- **(c)** **No `H_ref` is constructed or implied.** That needs a reference
  protocol, an independent development split and a pre-registered threshold — none
  of which exist — and adopting one is a separate human decision.

### R9 — Cost and disclosures

**Wall time 1.0 s**, zero GPU, run directly on a login node per the review. New file
only: `experiments/d015_per_pair.py`. No existing repo file modified; nothing
retrained, reselected, re-embedded (I6). All five sources read-only.

**Deliverables:** `results/D015/per_pair_k8.json` · `per_pair_k1.json` ·
`per_pair_gamma_named.json` (352 rows) · `summary_hardest8.json` ·
`summary_family_rollup.json` · `summary_moved_backward.json` ·
`s0_index_check.json`.

### R10 — For the supervisor deck, and for design side

**Lead with R1's decomposition, not a spread claim.** The one-line version:
*"the +0.41 pp hard-subset gain is 28 pairs up and 20 pairs down, and the gross
movement is three times the net."*

Three things design side should weigh:

1. **`hard_subset` as currently defined can move up while a third of its members
   move down.** That is a property of a 65-pair unweighted mean, not a defect in
   the methods — but any writeup quoting it should quote R1's decomposition beside
   it, or the number over-claims.
2. **The hardest pair gets resolvably worse under `coverage` than under the paper's
   8** (−0.116). This is the strongest single piece of evidence this project has
   that the selection methods are not uniformly better, and it survived being
   looked for rather than being stumbled on.
3. **R5 is a reporting trap worth recording**: any future `k`=1 comparison between
   `coverage` and `joint energy` is comparing a condition with itself. Both this D
   and D013/F3 have now hit it from different directions.

---
---

## Review — D015 results (post-hoc; D015 closed)

### For you (human)

**Verdict:** **CLOSED, 2026-09-22 — the core finding stands; one headline example
in `## R` names the wrong pair, corrected below.** Verified against
`results/D015/per_pair_k8.json` directly.

**What the correction is:** R2/R10's headline claim — *"the single hardest pair
in the set, Phi-3-medium-128k ↔ 4k, is 11.6 points worse under coverage than
paper8"* — attaches the −0.116 (resolved) number to the wrong pair. That number
belongs to **Phi-3-medium-128k ↔ Phi-3-mini-4k** (a 14B model vs. a 3.8B model,
cross-size, ranked **3rd** hardest) — not to **Phi-3-medium-128k ↔ Phi-3-medium-4k**
(the two same-size siblings, ranked **1st** hardest under every method, the pair
this project has called "hardest" since D001). The true hardest pair's own
coverage-vs-paper8 delta is **−0.056, CI [−0.160, +0.044] — not resolved.**
So: **we cannot say the project's named hardest pair got resolvably worse under
coverage.** A different, less-famous pair did.

**Everything else in `## R` checked out on independent recomputation:** the
28-up/20-down/17-flat redistribution counts and gross sums (joint−coverage) and
the 28/21/16 counts (coverage−paper8) are exactly reproduced from the raw JSON;
R3's rank-1/rank-2 table (the two named pairs rank hardest under all three
methods) is correct; R6's γ-curve numbers for Falcon3-10B/7B are correct. The
redistribution finding — a `hard_subset` gain built from offsetting gains and
losses — **is real and is this D's actual answer**; only one illustrative
example was mislabeled.

**Needs your input:** None.

**Follow-ups opened:** None — a one-line process note added below for TACC, not
a new D.

**Next:** Use the corrected framing below in place of R2/R10's original example
in any writeup or presentation. `PLAN.md`/`DECISIONS.md`/`FINDINGS.md` updated
to match.

---

### Technical log

**How this was found.** R2's table lists pair `"Phi-3-medium-128k ↔ 4k"` with
Δ=−0.116. Looked up that exact pair (`microsoft/Phi-3-medium-128k-instruct` +
`microsoft/Phi-3-medium-4k-instruct`) in `results/D015/per_pair_k8.json` directly:
`d_coverage_minus_paper8` = **{delta: −0.056, lo: −0.1601, hi: 0.044, resolved:
false}**. Different number, different resolution status. Cross-referenced
`results/D015/summary_moved_backward.json`'s row carrying delta=−0.116: its
`pair` field is `"microsoft/Phi-3-medium-128k-instruct | microsoft/Phi-3-mini-4k-instruct"`
— confirmed a genuine fourth Phi-3 variant exists in the 37-model universe
(`Phi-3-mini-4k-instruct`, params_b 3.8, distinct from `Phi-3-medium-4k-instruct`,
params_b 14.0 — checked against `results/D007/tensor_tok200.meta.json`'s model
list). This is a transcription slip in `## R`'s prose (the underlying
`per_pair_k8.json`/`summary_moved_backward.json` data is internally consistent
and not itself corrupted — the two pairs are stored as separate, correctly-valued
rows; only the written description in `## R` picked up one row's numbers under
the other row's more-famous name), not a join or indexing bug — S0/R4's
indexing check is unaffected.

**Independent recomputation of every other headline number in `## R`:**

```
joint_energy - coverage  (k=8):  up=28  down=20  flat=17  gross+=0.544  gross-=-0.276
coverage - paper8        (k=8):  up=28  down=21  flat=16  gross+=0.592  gross-=-0.548
rank 1 (all three methods): Phi-3-medium-128k ↔ Phi-3-medium-4k
rank 2 (all three methods): Falcon3-10B ↔ Falcon3-7B
```

All match `## R` exactly. **The aggregate redistribution finding (R1, R7) is
unaffected by the correction** — it does not depend on which specific pair
illustrates it.

**Corrected version of R2/R10's headline example, for citation:**
*"At k=8, `coverage` is resolvably worse than `paper8` on 6 of 65 structural
pairs (not the project's named hardest pair specifically) — the largest is
Phi-3-medium-128k ↔ Phi-3-mini-4k at −0.116 (CI [−0.212, −0.024]), a cross-size
Phi-3 pair ranked 3rd hardest, not the 1st-ranked medium-128k/medium-4k sibling
pair. The named hardest pair's own coverage-vs-paper8 delta (−0.056) does not
resolve at this sample size."* This is a weaker but still real claim — coverage
does measurably hurt some near-relative pairs relative to paper8 — and it
remains the strongest resolved-negative evidence in the project to date; it
just is not evidence about the specific pair D001 first flagged.

**R3's "rank 1/2 under every method" finding is unaffected and independently
re-verified** — it does not rely on the mislabeled row.

**Process note for TACC (not a new D, not urgent):** consider having
`experiments/d015_per_pair.py`-style scripts assert the pair name printed in any
prose/table matches the `pair` field of the row the numbers came from, or
generate `## R`'s narrative tables programmatically from the JSON rather than
transcribing by hand — this class of slip (right numbers, adjacent pair's name)
is exactly what a round-trip check would catch mechanically.

**Closing.** D015's own question — is the aggregate `hard_subset` gain broad,
concentrated, or does it hide backward movement — is answered: **it hides real
backward movement (R1/R7), confirmed independently**, and the "spread vs.
concentrated" half remains correctly labelled unanswerable at this resolution
(P1/F3). Closing with the one correction above; no reason to hold it open
further or rerun anything (I6 — this was a read-only reorganization throughout).

