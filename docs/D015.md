# D015 — Per-pair decomposition of `hard_subset`: is the gain spread across the 65 structural pairs, concentrated, or does any pair get worse?

```
id:          D015
source:      docs/REVIEW-2026-09-22-hard-pairs.md §8, §10.A (retrospective
             audit requested by the human, 2026-09-22): `hard_subset` is
             a single number averaged over 65 pairs; it is neither the
             worst pair nor a tail average, and per-pair behaviour has
             never been reported beyond two named pairs (Falcon3-10B/7B,
             Phi-3-medium-128k/4k) shown descriptively in that review.
             Human-approved 2026-09-22 ("2的提案非常合理,可以立刻写D").
status:      OPEN
gate:        REVIEW          # produces numbers a writeup could use;
             default per CLAUDE.md. Cost is trivial (no training), but
             the framing (what counts as "concentrated" vs "spread", how
             per-pair CIs are computed) should be checked before it's
             taken at face value, per this project's standing practice.
deps:        D007 (frozen tensor, for pair↔index mapping only), D009
             (paper8, cvar_max=coverage γ=0.1 trained runs, k=1..8),
             D010 (joint_energy trained runs, k=1..8), D012 (JointGreedy
             γ∈{1.0,0.25,0.1,0.05} trained runs), D013 (GreedyCover
             γ∈{1.0,0.5,0.25,0.1,0.05} trained runs) — all CLOSED. No
             dependency is rerun; this D reads their saved
             `per_hard_pair` arrays only (I6).
opened:      2026-09-22
closed:      —
downstream:  Directly informs whether the two previously-named hard
             pairs are representative of a broader pattern or outliers;
             a prerequisite for ever defining a frozen empirical hard set
             `H_ref` (not attempted here — see Constraints), which would
             itself be a later, separate, human-approved decision.
```

---

## D — The question

> **Break the single `hard_subset` number (mean two-logit restricted
> accuracy over the 65 structural near-relative pairs, `METHOD.md §8`)
> into its 65 pair-level components, for `paper8`, `coverage`
> (`cvar_max`, γ=0.1), and `joint energy`, at matched `k`. Does the
> ≈0.4–0.5pp aggregate gain (D010, k=8) come from most pairs improving a
> little, a few pairs improving a lot while the rest are flat, or some
> pairs improving while others get worse (redistribution the aggregate
> would hide)?**

### What is and is not known going in

- The aggregate `hard_subset` numbers are already reported (D010, k=8:
  paper8 .9637, coverage .9644, joint energy .9685) and are small in
  absolute terms — the entire random-to-oracle range on this metric is
  only ~7.5 points (D008's own finding, the reason D008 was INCONCLUSIVE
  by the letter).
- Every run in D009/D010/D012/D013 already stores a `per_hard_pair` list
  of 65 floats per run (verified present, 2026-09-22). No new training,
  selection, or generation is needed — this is a read-and-reorganize job.
- The two previously-named difficult pairs (Falcon3-10B/7B,
  Phi-3-medium-128k/4k) are **not** derivation pairs — checked against
  `results/D001/model_metadata.csv`: both have an empty `base` field
  (no declared fine-tune parent/child relationship between the two
  models in the pair). They match on `lineage` only (`falcon-3`,
  `phi-3` respectively) — i.e. they are siblings released in the same
  model family/generation, not one derived from the other. Of the full
  65 structural pairs (37-model universe), 6 also share a literal `base`
  field (true fine-tune-of-shared-parent pairs), and those 6 are a
  subset of the 65 `same_lineage` pairs, not a disjoint category, in
  this dataset.
- What is *not* known: whether the aggregate gain is broad or narrow: no
  per-pair breakdown beyond the two named pairs has been reported.
- **Added 2026-09-22, per the human's question about where "hard" comes
  from for the two named pairs.** Their difficulty label has three
  independent sources, of different strength, none of them yet a
  trained/frozen/systematic protocol: (1) **D001** — an unsupervised
  open-set nearest-template AUC statistic computed on the *paper's own
  shipped artifact* (`default/` pretrained templates over 52 models),
  before this project generated any corpus of its own; Falcon3-7B↔10B
  was "the hardest pair in the entire matrix" there, Phi-3-medium-4k↔128k
  the 4th-hardest. External to our pipeline, but not a trained
  classifier. (2) **D008** — a single-query proxy *oracle* (best-of-pool,
  not a selected chain) on our own frozen `S_energy` tensor/corpus:
  0.840 / 0.800. Same corpus D009–D014 use, so not independent of it,
  and the review's 2026-09-22 audit (§4) already flagged this oracle
  number as a different estimand from a trained k-query classifier — not
  a ceiling on it. (3) **D010's saved per-seed trained numbers**
  (coverage .656–.800, joint energy .732–.832) — real trained accuracy,
  but never assembled into a clean per-pair report until this D. No
  source here is a prospectively-frozen, independent-development-set,
  trained-classifier measurement — see `H_ref` in Constraints.

---

## What counts as an answer

Report, per pair, per method, at `k`=8 (the project's standard headline
budget) and `k`=1 (smallest budget, for contrast):

- mean accuracy across the already-run 5 seeds, with a bootstrap CI
  computed the same way the project already computes per-cell CIs
  (paired at the config-bootstrap level, per D009's convention) —
  **note going in that these will be noisier than the aggregate**: each
  pair's number comes from ~50 evaluation traces (2 models × 25 test
  configs) × 5 seeds, an order of magnitude less data than the 65-pair
  average. State this caveat in `R`, don't let a narrow per-pair CI be
  read as a null result.
- Δ(joint energy − coverage) and Δ(coverage − paper8) per pair, flagged
  by sign — **explicitly list any pair where Δ is negative** (joint
  energy or coverage does worse than the comparator), not just the
  positive ones.
- Where the two previously-named pairs rank in this list — are they the
  hardest under every method (consistent with "structurally hard
  regardless of selection"), or does their rank change by method
  (consistent with selection-algorithm-dependent difficulty)?

**Not required:** a hardness ranking presented as an adopted definition
(`H_ref`) — see Constraints. **Not required:** worst-class per-pair
analysis, or any new `γ`/`k` combination beyond what D009/D010/D012/D013
already ran.

**Also required, for presentation use (added 2026-09-22):** the full
65-row table is the audit artifact, not the thing to hand to a
supervisor. Produce, alongside it, three short derived views (S6) —
a plottable γ×`k` curve for the two named pairs, a hardest-8 table, and
a per-family rollup — each under ~10 rows, each clearly labelled as a
*subset or aggregation of* the full table, not a separate measurement.

---

## Required steps

**S0 — Confirm consistent pair indexing across the four source files.**
Each of D009/D010/D012/D013's runs was produced against the same frozen
37-model universe (I6), so `near_relative_pairs(models)`'s 65-entry
index→(model_a, model_b) mapping should be identical across all of
them. Verify this explicitly (e.g. assert the same `models` list, or
the same derived pair-name list, in each file's metadata) before joining
any numbers across files — a silent reordering would silently corrupt
every downstream row. Report the check's outcome in `R` even if it
merely confirms the expected.

**S1 — Core deliverable: the 65×3 table at `k`=8.** For each of the 65
pairs, report `paper8`, `coverage` (γ=0.1), `joint energy` mean accuracy
(5-seed mean + CI) and the two deltas above. Source: D009's `paper8` and
`cvar_max` runs at `k`=8, D010's `joint_energy` runs at `k`=8. Sort for
readability by `paper8`'s accuracy (hardest-under-paper8 first) —
label this sort as a display convenience, not a hardness claim.

**S2 — Contrast table at `k`=1.** Same construction, `k`=1, to see
whether concentration/spread looks different at the smallest budget.

**S3 — Required: the two named pairs' full γ×`k` curve.** D012
(JointGreedy, γ∈{1.0,0.25,0.1,0.05}) and D013 (GreedyCover,
γ∈{1.0,0.5,0.25,0.1,0.05}) already have `per_hard_pair` stored for every
γ×`k` cell they ran (no new computation — pure extraction + the same
per-cell CI procedure as S1). For **Falcon3-10B/7B** and
**Phi-3-medium-128k/4k** only, report accuracy at every stored (γ, `k`)
point, both algorithms, in a shape ready to plot as a line chart
(x=`k`, one line per γ, one panel per algorithm per pair — or
equivalently a long-format table with columns `pair, algorithm, gamma,
k, mean, ci_lo, ci_hi`). **Do not build a denser pair×γ×`k` grid than
what D012/D013 already ran** — this reports exactly those stored cells,
nothing interpolated or newly computed; label every cell with which
source D it came from.

**S4 — Explicitly separate three things in `R`, per the review's
typology (do not conflate them):** (a) this D's 65-pair table is the
existing structural set `N` (metadata-defined, fixed since D001/D008
specifically to avoid scoring a selection algorithm against a hardness
definition drawn from its own tensor) — not a hardness-ranked
benchmark; (b) the two previously-named pairs, shown in context among
the other 63, not cherry-picked; (c) note explicitly that this D does
**not** construct a frozen empirical hard set (`H_ref`) — that requires
a reference protocol, an independent development split, and a
pre-registered threshold, none of which exist yet, and adopting one as
a primary endpoint is a separate human decision.

**S5 — Report descriptively; reuse existing statistics code.** No new
significance-testing framework — the same bootstrap/CI procedure already
used for aggregate metrics in D009–D014, applied per pair instead of
averaged over pairs. Do not invent a new test.

**S6 — Presentation-ready summary views, each derived from S1–S3's data,
not separately measured.** All under ~10 rows:

- **Hardest-8 table** — the 8 pairs with the lowest `paper8` accuracy at
  `k`=8 (from S1), all three methods, same columns as the full table.
  Confirm where the two named pairs land in this list (should be at or
  near the top, given they were flagged hardest by D001/D008 — report
  the actual rank, do not assume it).
- **Family rollup** — group the 65 pairs by `lineage` tag (e.g. all
  `llama-3`-tagged pairs, all `qwen2`-tagged pairs, …) and report each
  family's mean accuracy per method (mean of its member pairs' S1
  numbers, unweighted within family) plus member count. State the
  weighting explicitly (larger families get one row, same as small
  ones) — this is a display choice, not a claim that families should be
  weighted equally in `hard_subset` itself.
- **Moved-backward table** — every pair (if any) where
  Δ(joint energy − coverage) or Δ(coverage − paper8) is negative with
  the CI excluding zero (S1's flagged list, filtered to only the
  resolved-negative rows). Expected to be short or empty; report
  "none" explicitly if so, rather than omitting the table.
- **S3's two-pair γ×`k` curve** already satisfies the "presentation
  view" bar on its own (≤5 γ values × 8 `k` values per pair per
  algorithm) — no further compression needed there.

---

## Constraints

**Applicable invariants:** I6 (every number sourced from
D009/D010/D012/D013's already-frozen runs; no re-selection, no
re-training, no re-generation, no re-embedding).

**Out of scope:** constructing `H_ref` (S4c); any new `γ` or `k` value;
worst-class per-pair analysis; any claim that a pair is "solved" or
"intrinsically hard" from these numbers alone (per-pair CIs will be wide
— report them, do not round them away); editing `METHOD.md`.

**Cost:** ~0 GPU time — a data-processing script over already-saved
JSON, plus bootstrap CIs (CPU, minutes at most). **State the measured
wall time in `R`** anyway, per standing project practice of always
reporting it, however small.

---

## Deliverables

| | |
|---|---|
| `results/D015/per_pair_k8.json` | S1: 65-row table, 3 methods, means + CIs + deltas |
| `results/D015/per_pair_k1.json` | S2: same, `k`=1 |
| `results/D015/per_pair_gamma_named.json` | S3: the 2 named pairs across the full stored γ×`k` grid, both algorithms, plot-ready long format |
| `results/D015/summary_hardest8.json` | S6: hardest-8 table |
| `results/D015/summary_family_rollup.json` | S6: per-lineage rollup |
| `results/D015/summary_moved_backward.json` | S6: any resolved-negative pairs (or an explicit empty result) |
| `## R` below | the rendered tables (full + S6 summaries), the S0 indexing check, and the S4 typology separation |

---

## Notes for whoever writes P

- **This is the cheapest D in the project by GPU cost (zero) — the risk
  here is entirely in bookkeeping (S0's indexing check) and in framing
  (S4), not in compute.** Spend the plan's care there, not on run
  efficiency.
- **Report every pair, including the boring ones, in the full S1/S2
  tables.** A table that only shows pairs where something moved would
  defeat the point (that is exactly the "hard_subset hides
  redistribution" concern this D exists to check). **S6's summaries are
  additional, not a replacement** — they exist because the human wants a
  short version for a supervisor presentation; keep the full 65-row
  data too.
- **If S0's indexing check fails** (pair order differs across source
  files), stop and report it in `R` rather than guessing a fix — that
  would be a data-integrity issue in prior D's, not something to patch
  silently inside this one.
- **P must be executable by a fresh session with no conversation
  context**, per the standing rule in `CLAUDE.md`.

---
---
