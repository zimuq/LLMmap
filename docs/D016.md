# D016 — Do genuinely hard pairs exist outside the 65 structural near-relative pairs, under real trained-classifier evaluation?

```
id:          D016
source:      FINDINGS.md "reformulating hard_subset" entries (2026-09-22/23)
             and their 2026-09-23 addendum; human-approved 2026-09-23
             ("是的,起草这个D"). Design-side found that the only
             already-computed per-pair statistic spanning all 666 pairs
             (`results/D008/identifiability_frontier.json`'s T1/T2/T3
             tiers) is the exact population D011 already showed is
             substantially an estimator artifact, and declined to reuse
             it. This D asks the question properly instead.
status:      OPEN
gate:        REVIEW          # produces numbers that could revise
             METHOD.md §8 (DECISIONS.md D7) and feed a writeup claim —
             default per CLAUDE.md
deps:        D007 (frozen S_energy tensor, I6), D008 (`hard_ceiling`-style
             tensor-proxy code, `two_way_correct`/`pair_index` in
             `d008_lib.py`), D009 (paper8/coverage trained checkpoints,
             k=1..8), D010 (joint_energy trained checkpoints, k=1..8),
             D011 (why a build-selected single-query proxy is untrustworthy
             — the failure mode this D must not repeat), D015 (per-pair
             extraction pattern and its `logit_stats`/config-bootstrap
             conventions) — all CLOSED.
opened:      2026-09-23
closed:      —
downstream:  Directly answers DECISIONS.md D7 (should `hard_subset` be
             redefined) and completes the FINDINGS.md 2026-09-22
             proxy-vs-trained correlation check at its intended scale (all
             666 pairs, not just the 65 structural ones flagged as a
             limit at the time).
```

---

## D — The question

> **Two things, using the SAME underlying data:**
> **(a)** Extend the trained classifier's real two-logit-restricted
> accuracy from the 65 structural near-relative pairs to as much of the
> full 666-pair population as feasible, for `paper8`, `coverage` (γ=0.1),
> `joint energy` (γ=0.1), at `k`=8. Are there pairs *outside* the
> structural set that are as hard, or harder, than the worst structural
> pairs (Phi-3-medium-128k/4k, Falcon3-10B/7B)?
> **(b)** Extend the tensor-level proxy statistic (D008's
> `hard_ceiling`-style `median_query`/`oracle`/`best_build_query`) to the
> same full population, and correlate it against (a)'s real trained
> accuracy — the rigorous version of the 2026-09-22 design-side check that
> was limited to 65 pairs at the time.

### Why this is not a repeat of D008/D011's mistake

D011 showed `results/D008/identifiability_frontier.json`'s T1/T2/T3 tiers
are substantially an estimator artifact: their `best_build_query`
statistic is *one single query*, chosen via build-side data, evaluated on
test — and for a median pair in that tier it generalized so poorly
(0.54, worse than a random pool query at 0.82) that a real trained
classifier using any properly-selected multi-query chain resolved 32/32
of them. **This D does not use that statistic as ground truth for
hardness.** It asks TACC to compute the same thing D009–D015 already
compute and trust for the 65 structural pairs — **a real trained
classifier's argmax-restricted-to-two-classes accuracy, from an actually
trained `k`=8 model** — for the *other* 601 pairs too. That is not a
proxy; it is the same measurement this project has used as ground truth
since D009, just applied to a wider population. Part (b)'s tensor proxy
(`median_query`, an accuracy averaged over the whole 259-query pool, not
a single circularly-chosen query) is retained *only* as something to
correlate against the real number, per the 2026-09-22 check's own
methodology — never as a substitute for it.

### What is and is not known going in

- `results/D009/runs.json` and `results/D010/metrics_by_k.json` each
  record a `best_ckpt` filename per run — a trained model was saved. **It
  is not known whether these checkpoint files still exist on disk** in
  TACC's environment; this repo does not track them (too large, binary).
  This is the first thing to check (S0) and it changes this D's cost by
  an order of magnitude either way.
- If checkpoints exist: extending (a) to all 666 pairs needs **no
  retraining** — one forward pass per already-trained model over the
  already-frozen test set recovers the full 37-way logits per trace, and
  restricting to any pair of columns instead of just the 65 near-relative
  ones is a cheap, purely arithmetic extension of `d009_lib.py:logit_stats`
  (same function, wider `hard_pairs` argument).
- If checkpoints are gone: recovering (a) needs **retraining**, which is
  a materially different cost and a design-side re-scoping decision (see
  Constraints) — not something to do silently under this D's current gate.
- (b)'s tensor-level extension needs no checkpoint at all — it is the
  same computation as `experiments/d008_hard_ceiling.py`, with
  `pair_index(models)` (all 666) in place of `near_relative_pairs(models)`
  (65). Purely reads the already-frozen `S_energy` tensor and
  `build_dq`'s already-frozen distance matrices (I6) — no new embeddings,
  no new generation.

---

## What counts as an answer

**For (a):** report, for each of `paper8`/`coverage`/`joint energy` at
`k`=8, the full-population distribution of two-logit-restricted accuracy
(not just a mean) — at minimum: the worst 10 pairs overall, whether any
of them are outside the structural 65-set, and how their accuracy compares
to the known worst structural pairs (Phi-3-medium-128k/4k: .656–.732;
Falcon3-10B/7B: .800–.832). State explicitly whether the structural set
already contains the objectively hardest pairs in the universe, or misses
some.

**For (b):** report the Spearman correlation between the tensor proxy and
(a)'s real trained accuracy, across all 666 pairs, with a bootstrap CI —
the 2026-09-22 check's own three caveats (n=65 only; a proxy-accuracy
statistic, not the literal CVaR-tail objective value; no CI on the
correlation) should each be resolved or explicitly re-stated as still
open.

**Not required:** retraining anything if checkpoints are gone (report the
cost/scope question back to design-side instead, per Constraints).
**Not required:** re-deriving `worst_class`/`hard_subset`/`mean_top1` —
those are unchanged; only the pair-restricted view is new. **Not
required:** extending to `k` values other than 8, or to D012/D013's γ
grid — flag as a future cheap follow-up if this D's `k`=8 result is
informative enough to want more.

---

## Required steps

**S0 — Checkpoint availability check, before anything else.** For each of
the 15 runs behind `paper8`/`coverage`/`joint_energy` at `k`=8 (3
conditions × 5 seeds), check whether `best_ckpt`'s file still exists and
loads. Report the count that do. **If fewer than 5 per condition are
available, stop and report to design-side before proceeding** — this
D's Call 1 (below) needs a design-side decision on how to proceed with a
reduced-seed or retrained set, not a silent substitution.

**S1 — Recover full 37-way logits on the test set from each available
checkpoint.** One forward pass per checkpoint (no training). Assert the
recovered `mean_top1`/`per_model` reproduce the already-stored values for
that run (a correctness check against a known-good number, catching a
mismatched checkpoint/test-set pairing before it propagates).

**S2 — Compute two-logit-restricted accuracy for all `pair_index(models)`
pairs (666, or as many as models permit) from S1's logits**, using
`d009_lib.py:logit_stats`'s existing restriction logic with the wider
pair list — same config-level bootstrap-CI convention as D015 (paired
draws, `n_boot` consistent with prior D's). Report the full distribution,
not just a mean; flag the worst 10 and whether each is inside or outside
the structural 65-set.

**S3 — Extend the tensor-level proxy to the same 666 pairs**, adapting
`experiments/d008_hard_ceiling.py` (`median_query`, `best_build_query`,
`oracle` — retain all three, as before, and continue treating `oracle` as
the least trustworthy per its own docstring) with `pair_index(models)` in
place of `near_relative_pairs(models)`. Frozen tensor and distance
matrices only (I6) — no new embeddings or generation.

**S4 — Correlate S2 against S3**, Spearman with a bootstrap CI, across
all 666 pairs, for each of the three conditions independently (matching
the 2026-09-22 check's structure) — report whether the tail-level
agreement and mid-ranking disagreement pattern found on the 65-pair
sample holds, sharpens, or changes at full scale.

**S5 — Write `## R`:** the worst-10-pairs table (S2), explicitly marking
structural-vs-non-structural membership; the correlation table (S4); and
a direct answer to "does the structural 65-pair set already contain the
objectively hardest pairs, or does real difficulty exist outside it."

---

## Constraints

**Applicable invariants:** I6 (same frozen `S_energy` tensor, same frozen
test/build splits, same already-trained checkpoints — nothing
regenerated, nothing retrained under this D's current gate).

**Out of scope, explicitly:** retraining any condition/seed to replace a
missing checkpoint — if S0 finds checkpoints missing, **stop and report
it in `## R`**, with the count available and an estimated retraining cost,
and let design-side decide whether a reduced-seed analysis, a resampled
subset of the 601 non-structural pairs, or a full retrain is the right
next step. Do not decide this unilaterally and proceed. Also out of
scope: any change to `k`, γ, or the three conditions already used
throughout D009–D015; editing `METHOD.md`; adopting a new `hard_subset`
definition (that is DECISIONS.md D7, a separate human decision this D's
`## R` will inform, not make).

**Cost:** if checkpoints exist, expect this to be materially cheaper than
any prior training D — inference only, no backward pass, on already-cached
test data. **State the measured cost in `## R`** regardless, per standing
project practice (D002/D006/D007/D009/D010/D012's precedent of measuring
before assuming).

---

## Deliverables

| | |
|---|---|
| `results/D016/checkpoint_availability.json` | S0 |
| `results/D016/full_pair_trained_accuracy.json` | S2: two-logit-restricted accuracy, all ~666 pairs, 3 conditions, with structural-membership flag per pair |
| `results/D016/full_pair_tensor_proxy.json` | S3: `median_query`/`best_build_query`/`oracle`, all ~666 pairs |
| `results/D016/full_pair_correlation.json` | S4: Spearman + bootstrap CI, per condition |
| `## R` below | S5's synthesis and direct answer |

---

## Notes for whoever writes P

- **Call 1 (if S0 finds missing checkpoints): how to proceed** — must be
  raised in P1 and resolved by design-side review before S1–S5 run on a
  reduced or substituted basis. Do not guess at an acceptable seed count.
- **This D deliberately reuses D009/D010's already-trained checkpoints
  rather than retraining** — the entire point is to avoid the cost and
  the I6 risk of a fresh training run; if that assumption breaks (S0),
  the D's cost profile changes and needs a fresh look, not a workaround.
- **Report the full worst-10 table even if every one of them turns out to
  be inside the structural 65-set** — a null result here (structural set
  already captures the hardest pairs) is exactly as informative as a
  positive one, and directly closes the open question from the
  2026-09-22 FINDINGS entry either way.
- **P must be executable by a fresh session with no conversation
  context**, per the standing rule in `CLAUDE.md`.

---
---

## P — Execution plan

<!-- TACC writes below this line -->

### P1

**Status: PROPOSED — awaiting review.** Full plan:
[`plans/D016-P1.md`](plans/D016-P1.md). S0 (required first), S3 (explicitly
checkpoint-independent) and a **one-run measurement for Call 1** have been run —
jobs 1018738 and 1018750, **37 s of compute total**. S1, S2, S4 have not. **No
existing repo file modified.**

**S0's gate FAILS — 1 usable checkpoint per condition, not 5 — but the premise
behind D016's escalation does not hold.**

| condition | runs in source | checkpoints on disk | load **and** reproduce stored `mean_top1` |
|---|---|---|---|
| `paper8` | 5 | **1** | **1** |
| `coverage` | 5 | **1** | **1** |
| `joint_energy` | 5 | **1** | **1** |

**Nothing was lost.** D009 and D010 only ever *wrote* seed 0 — their loops save a
checkpoint `if r_ == 0`. The files present are exactly the files ever created.
D016 says *"it is not known whether these checkpoint files still exist"*, which
assumes deletion; the correct statement is that the other 12 **were never saved**.
No corruption, no partial state, nothing to recover. The three that exist load and
reproduce their stored `mean_top1` to 1e-6, so S1's correctness check passes on
every checkpoint that exists — only the seed count is short.

**Call 1 — measured, and it collapses.** D016 asks design side to choose between a
reduced-seed analysis, a subsampled pair set, or a retrain, on the assumption that
retraining is *"a materially different cost"* and changes cost *"by an order of
magnitude."* Measured on one run — `paper8` `k`=8 **seed 1**, a seed with no
checkpoint:

| | |
|---|---|
| retrain wall | **9.8 s** |
| stored `mean_top1` | 0.838919 |
| retrained `mean_top1` | **0.838919** |
| \|difference\| | **0.000000 — bit-exact** |
| **12 missing runs** | **2.0 minutes** |

`pl.seed_everything(seed, workers=True)` + a fixed `hparams` hash + frozen inputs
makes these runs reproducible, so **a retrained seed *is* the seed D009/D010
reported, not a resample of it.** There is no statistical difference between "the
checkpoints existed" and "we retrain them", and the cost difference is **2
minutes**. **Proposal: retrain the 12 and proceed on the full 5-seed basis D016
specifies** — raised and measured here, not done. Plus two one-line asks: **save
all 15 checkpoints this time** (~550 MB, gitignored), and note that
`d009_train.py`'s `if r_ == 0` is why this Call existed at all (flagged, not
edited — prior D's file, outside this D's scope).

**Part 0 — S3 is done and already answers half the question at proxy level.**
`median_query` (averaged over all 259 queries — the statistic D011 did *not*
discredit) over all 666 pairs: **all 0.8686 · structural 0.8157 (n=65) ·
non-structural 0.8743 (n=601)**. The structural set is genuinely harder on average,
so it is not an arbitrary label. **But 4 of the 10 hardest pairs are
non-structural**: `DeciLM-7B ↔ Mistral-v0.1`, `Llama-2-7b-chat ↔ Mistral-v0.2`,
`Phi-3-medium-128k ↔ Mistral-v0.1`, `Phi-3-medium-4k ↔ Mistral-v0.1` — all at
`median_query` 0.720, against Phi-3-medium-128k/4k's 0.560 and Falcon3's 0.660.
**Stated as a limitation, not smuggled as a result:** this is the proxy, D009/R2
measured the proxy→trained shift as condition-dependent, and S2's trained numbers
are what answer the D.

**F1 — the 4 non-structural hard pairs are all cross-family.** None shares a
`lineage` tag. If they survive S2 the finding is not "the structural definition
missed some near-relatives" but **"difficulty is not only a near-relative
phenomenon"** — a materially different claim, bearing directly on `DECISIONS.md`
D7. Proposal: report lineage alongside the structural flag in S2's table; costs
nothing.

**F2 — do not price S2 against S3's runtime.** S3 took 27 s because it evaluates
259 queries per pair. S2 needs one forward pass per run (15 total), after which
restricting to two columns is arithmetic over a `(925, 37)` array — under a minute.

**F3 — the per-pair bootstrap needs `cnt_pair` at 666 wide, which no stored
artifact has.** D015 reused the stored (65, 25) arrays; for 666 pairs it must be
rebuilt from S1's logits. Proposal: recompute inside S2 via `logit_stats`'s own
restriction logic with the wider pair list (what D016/S2 already specifies), then
apply D015's paired config bootstrap unchanged.

**Verification worth naming:** S2's 65 structural rows must reproduce D015's
`per_pair_k8.json` values — a direct cross-D check that the wider computation did
not change the narrower one.

**Cost:** ≈3 min total (2.0 retrain + <1 S1/S2/S4); S0+S3 already cost 37 s.

**Sequencing:** S1, S2, S4 blocked on Call 1 only. S0 and S3 complete.

---
---

## Review — D016 / P1

### For you (human)

**Verdict:** APPROVED WITH AMENDMENTS. Call 1 (retrain the 12 missing
seeds — 2 minutes, and independently verified bit-exact) and F1/F2/F3 all
accepted. One correction to Part 0's prose (a pair-naming typo, numbers
unaffected); one process note for S2.

**What changed:**
- Part 0 named two of its four "hardest non-structural pairs" as paired
  with `Mistral-7B-Instruct-v0.1`; checked against the raw data, both are
  actually paired with `Mistral-7B-Instruct-v0.3`. The count (4 of 10) and
  the other two names are correct — I reproduced TACC's exact sort and got
  the identical 4 pairs. A transcription slip in the writeup, not the
  computation; flagged before it could get cited under the wrong name.
- Added a note for S2: this rank-10 cut sits inside a 7-way tie at
  `median_query`=0.720 (2 more non-structural pairs, including
  `openchat-3.6-8b↔openchat_3.5`, tie at the same value one rank below).
  TACC's fixed pair-order tie-break is fine and reproducible, but S2's
  real-accuracy version should say explicitly how ties are broken and
  should not imply "exactly 10" is a principled cutoff.

**Needs your input:** None.

**Follow-ups opened:** None.

**Next:** TACC executes S1, S2, S4 on the full 5-seed basis (12 retrains
+ the original 3 loaded checkpoints).

---

### Technical log

**Call 1 — approved, and it was worth measuring rather than guessing.**
D016's own Constraints assumed retraining would be "a materially different
cost" needing a design-side re-scoping decision — that assumption was
wrong, and TACC was right to test it empirically before escalating back.
Verified independently: `results/D016/checkpoint_availability.json` shows
run 0's checkpoint for `paper8`/`coverage`/`joint_energy` all load and
reproduce their stored `mean_top1` to `abs_diff: 0.0`; the `if r == 0`
checkpoint-saving line is real (`experiments/d009_train.py:156`,
`d010_evaluate.py:69`, `d012_evaluate.py:87`, `d013_evaluate.py:92`) — this
is a genuine "never saved," not a deletion, exactly as TACC diagnosed. The
one-run retrain measurement (`results/D016/call1_cost_measurement.json`)
reproduces `paper8` `k`=8 seed 1's stored `mean_top1` bit-exactly
(0.8389189189189189 both times) in 9.8s. `pl.seed_everything(seed,
workers=True)` plus the asserted-identical `hparams` hash plus I6's frozen
inputs makes this a sound basis for treating a retrained seed as *the*
reported seed rather than a new draw — proceed on the full 5-seed basis
as proposed.

**Part 0 verification.** Independently recomputed `results/D016/full_pair_tensor_proxy.json`'s
summary: all=0.8686, structural=0.8157 (n=65), non-structural=0.8743
(n=601) — matches exactly. Re-ran TACC's own `sorted(rows, key=lambda r:
r["median_query"])[:10]` (`experiments/d016_s0_s3.py:127`) against the
stored rows and got the identical 10 pairs, 4 non-structural — the *count*
and *which 4 rows* are both correct and reproducible. Only the prose
mislabeled two of those four pairs' partner model (`v0.1` written for
`v0.3`); corrected above. F1's cross-family claim ("none shares a lineage
tag") independently checked against `results/D001/model_metadata.csv` for
all 5 non-structural pairs in the tied group (including the one Part 0
didn't name) — holds for all 5, so the substantive finding is sound and,
if anything, one pair stronger than stated.

**F1/F2/F3 — accepted as proposed.** F1's framing (report lineage
alongside the structural flag in S2, "difficulty is not only a
near-relative phenomenon" is the material claim if these survive at the
trained level, not "the structural definition missed some near-relatives")
is exactly the right distinction for D7. F2's cost estimate (S2 is one
forward pass per checkpoint, then arithmetic — not priced against S3's
per-pair 259-query evaluation) is sound. F3's plan (rebuild `cnt_pair` at
666-wide from S1's logits, reusing D015's paired config-bootstrap
unchanged) is the correct generalization, and the named verification (S2's
65 structural rows must reproduce `results/D015/per_pair_k8.json` exactly)
is the right built-in check — **treat any mismatch there as a stop
condition, not a rounding note.**

**Two one-line asks, both reasonable, no action needed from design side:**
saving all 15 checkpoints this time (~550MB, gitignored) is a sensible
process improvement for the next D that needs them; the `if r == 0` line
is correctly flagged as outside this D's scope (a prior D's file) rather
than edited in passing.

**No amendments to S1/S2/S4 as specified in D016.** Proceed as planned,
on the full 5-seed basis.

---

## R — Results summary

<!-- TACC writes below this line -->
