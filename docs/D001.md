# D001 — Does exploitable hard-pair structure actually exist?

```
id:          D001
source:      TODO T0.1, T0.2
status:      OPEN
gate:        REVIEW          # plan must be approved before execution
opened:      <date>
closed:      —
downstream:  D002 (model universe selection) depends on this answer.
             A NEGATIVE result revises METHOD §2 and possibly invariant I1 —
             it does not merely remove a TODO item.
```

---

## D — The question

> **Are some model pairs substantially harder to separate than others, and is that
> hardness *structured* — concentrated in fine-tune / same-family relationships —
> rather than spread uniformly across pairs?**

### Why this gates everything

CDQD rests on two premises (`METHOD.md` §2):

1. **A heavy hard tail exists.** If all pairs are comparably separable, then
   `CVaR_γ` is numerically equal to the mean, our objective is identical to
   LLMmap's, and every downstream comparison is a null experiment **that produces
   no error and no warning** (`METHOD.md` §6.4).
2. **The hard tail is structured.** Targeted generation (`METHOD.md` §5.4 step D)
   conditions a generator on a *confusion cluster*. If hard pairs are unstructured
   noise, there is no coherent cluster to condition on, and step D has no target.

Premise 1 alone justifies a new objective. Premise 1 **and** 2 justify the full
loop. If neither holds, we should know that now — before spending ~10⁵ generations
on a corpus.

### Prior expectation (state it now, so we can be wrong on record)

The paper's own worst case is Meta-Llama-3-70B-Instruct at 84%, and the confusion
matrix attributes it to Smaug-Llama-3-70B-Instruct — *a fine-tune of that model*.
We expect this to be an instance of a general pattern, not an isolated case.

**We are pre-registering this expectation deliberately.** If the data contradicts
it, that is a finding, not a failure.

---

## What counts as an answer

| Outcome | Criterion |
|---|---|
| **CONFIRMS** | The pairwise separability distribution has a distinct left tail, **and** the bottom decile is enriched in structurally-related pairs (base↔fine-tune, adjacent versions, same family) relative to their base rate among all pairs. |
| **FALSIFIES** | The bottom decile is statistically indistinguishable from a random draw over all pairs — i.e. hardness carries no structural signal. |
| **INCONCLUSIVE** | Too few structurally-related pairs exist among the shipped models to detect enrichment, or the enrichment estimate's confidence interval spans "no effect". → report; do not force a verdict. |

**Report the actual distribution regardless of verdict.** The shape of that
distribution is itself an input to choosing `γ` (C1) and `θ` (C5).

---

## Required measurements

**M1 — Pairwise separability matrix** over all models shipped with the released
pretrained open-set model (`./data/pretrained_models/default`, which ships
behavioral templates for 52 LLMs). Whatever quantity that model naturally exposes
for comparing two models is acceptable here; state precisely what was used.

**M2 — Distribution** of pairwise separability: histogram + the 5th/10th/25th/50th
percentiles.

**M3 — Structural annotation of the bottom decile.** For each hard pair, label:
same base model (one is a fine-tune of the other) · adjacent versions in one
family · same organisation · same architecture family · same tokenizer · unrelated.
Report an enrichment statistic vs the base rate over all pairs, with a confidence
interval.

**M4 — The motivating case.** Does Meta-Llama-3-70B-Instruct ↔
Smaug-Llama-3-70B-Instruct land in the hard tail? Report its percentile rank.

**M5 — The ≤14B subset.** Restrict to models ≤14B and repeat M2–M3.
**This is the measurement that actually feeds D002.** Compute limits may force us
to exclude 70B-class models from the universe; if hard pairs exist *only* among
large models, invariant I1 becomes hard to satisfy and the universe decision
changes shape. Report the best ≤14B candidate confusion clusters explicitly.

---

## Constraints

**Applicable invariants:** none of I1–I7 bind here — this D *precedes* corpus
construction. But M5 exists specifically to determine whether I1 is satisfiable.

**Interpretation limits — carry these into R:**

- The released repo is **LLMmap0.2**, a PyTorch rewrite the README explicitly
  states is *not* a one-to-one conversion of the paper's implementation. Numbers
  may differ from published ones. Record what we observe as *our* baseline; do not
  reconcile against the paper.
- Separability measured through this model reflects hardness **under the paper's
  existing 8 queries**, not hardness in principle. That is exactly what we want —
  it tells us where the current strategy fails — but it must not be reported as
  "these models are intrinsically indistinguishable." That stronger claim belongs
  to the identifiability frontier (T2.4), which requires the full pool.
- Ground truth for "is X a fine-tune of Y" comes from model cards and naming.
  Flag any pair where the relationship is inferred rather than documented.

**Out of scope:** do not build a corpus, do not train anything, do not implement
the separability tensor or GreedyCover. This D answers a premise question only.

---

## Deliverables

| | |
|---|---|
| `results/D001/pairwise.csv` | full matrix, machine-readable |
| `results/D001/distribution.png` | M2 |
| `results/D001/hard_pairs.csv` | bottom decile with M3 annotations |
| `results/D001/subset_14b.csv` | M5 |
| `## R` below | qualitative verdict + quantitative summary + confidence |
| A recommendation for **A1** | which models the universe should contain, given M5 |

---

## Notes for whoever writes P

Deliberately unspecified, because they are yours to determine from the actual repo:

- **How a "model template" in the released open-set model maps to a comparable
  quantity.** `setup_templates.py` and `add_new_template.py --num_prompt_confs`
  suggest a template aggregates over ~100 prompt configurations. Whether that
  yields a centroid, a set of signatures, or something else — **inspect the code,
  do not assume.** If it turns out a template is a single vector, say so: it means
  intra-model spread has already been collapsed, which changes what M1 measures
  and is worth flagging.
- Whether the shipped model can be used as-is or needs traces generated first.
- How to define the enrichment statistic and its CI.

**P must include** as prerequisite steps: environment bring-up on Vista, and
running `test_model.py ./data/pretrained_models/default -k 3` as a reproduction
baseline. Flag early if the platform is aarch64 (GH200) and any required package
lacks an ARM build — that finding changes D002, not just the code.

**P must be executable by a fresh session with no conversation context.** Assume
the approving human returns days later and the session that wrote it is gone.

---
---

## P — Execution plan

> *Written by TACC-side agent. Multiple versions allowed: `### P1`, `### P1.a`
> (revision after rejection), `### P2` (a second plan under the same D).
> Each version states: steps, expected wall-clock and GPU-hours, outputs,
> and what it will do if a step fails.*

<!-- TACC writes below this line -->

### P1

**Status: PROPOSED — awaiting review.** Full plan: [`plans/D001-P1.md`](plans/D001-P1.md)
(kept as a separate file for reviewability; this section carries the summary.)

**Three repo questions D001 left open are now answered:**

- **A template is a single 384-dim centroid — intra-model spread is already
  discarded** (`LLMmap/templates.py:58`, `LLMmap/inference.py:188`; 52 keys × 384
  floats, unnormalized). D001 asked for this to be flagged if true. It is true, and
  it means **centroid distance is not a separability measure** — M1 must be measured
  from traces, not centroid geometry.
- **The only comparison quantity natively exposed is trace→centroid Euclidean
  distance**, uncalibrated (the siamese distance→probability head is absent from the
  checkpoint).
- **The shipped model is usable as-is, no trace generation needed.** 3536 test
  entries, perfectly balanced at 68 per model × 52 models; test `llm` values,
  `templates.json` keys, and `conf['llms_map']` keys are three identical sets.

**Metric.** For pair (A,B), score traces by `Δ = d(trace,T_B) − d(trace,T_A)` and
take the **AUC** separating A-traces from B-traces — symmetric, threshold-free
(required, since distances are uncalibrated), bounded, standard nonparametric CI.
Centroid distance is reported alongside so the first bullet is *evidenced*, not
merely asserted. All 1326 pairs derive from one `(3536 × 52)` distance matrix, so
the expensive embedding pass runs once.

**Steps.** S0 prereqs + full-test-set `test_model.py` baseline · S1 feature
extraction (reusing `load_datasets` + `infer_features`; cached to `.npy`) · S2
template provenance check · S3 M1/M2 · S4 M3 annotation + model-level bootstrap +
permutation test · S5 M4 · S6 M5 ≤14B subset · S7 write R and A1.

**Cost.** One `gh-dev` Slurm job, `-t 02:00:00`; **≈25–40 min wall-clock, ~0
GPU-hours** (see platform note). S2–S7 re-run off cache in seconds.

**Platform flag D001 asked for early.** aarch64/GH200 is fine — every dependency has
an ARM build. Two aarch64 blockers were found and are already fixed on `main`
(`torch.load()` ignored its `device` arg and crashed on the MPS-saved checkpoint;
`accelerate` missing from `requirements.txt`). Installed torch is `2.7.1+cpu` and
Slurm advertises no GRES on `gh`/`gh-dev`, so this runs on CPU. **Not a D002-level
finding.** Only new dependency: `matplotlib`.

**Pre-registered risk.** Only ~8–10 documented base↔fine-tune pairs exist among
these 52 models, so expected count in a 10% decile under the null is ~1 — the decile
test has power for large effects only. Mitigated by also reporting a broader
"structurally related" union label and a Mann-Whitney test over the full
related-vs-unrelated distributions. If CIs still span "no effect", the plan reports
**INCONCLUSIVE** rather than forcing a verdict.

**Blocking-ish open item.** `METHOD.md`, the TODO list, and invariants **I1–I7 are
not in this repo** — only `D001.md` was pushed. M5 exists specifically to test
whether I1 is satisfiable, and P must recommend A1, but neither I1's text nor the
A-series format is available. P1 therefore executes M5 in full and reports the ≤14B
findings **descriptively**, leaving the I1 judgment to the design side. Push those
docs and S6/S7 can tie the result to I1 directly.

---
---

## R — Results summary

> *Written by TACC-side agent. Concise. Full data lives in `results/D001/`.*

```
verdict:       FALSIFIES  (on the amended criterion's CVaR clause only -- SPLIT, read below)
confidence:    high on the measurement, LOW on its generalization -- the falsifying
               statistic is computed on a variable that is censored at its ceiling
               for 88.2% of pairs, so it characterises the instrument, not the population
impact_level:  METHOD  (may escalate to INVARIANT -- design-side call, see R6)
plan:          P1 (approved with amendments), amendments A1/A2/A3 all applied
data:          results/D001/
```

**The two premises separate cleanly, and they disagree.**

| Premise | Result |
|---|---|
| **2 — hardness is structured** | **CONFIRMED, decisively.** 11 of the 12 hardest pairs are near-relatives. |
| **1 — a heavy hard tail exists** | **NOT SUPPORTED.** M6 = 0.9966; 88.2% of pairs sit exactly at AUC 1.0. |

### R1 — Headline numbers (all 52 models, 1326 pairs, 3536 test traces)

| | |
|---|---|
| mean separability | 0.99962 |
| median / p25 | 1.0 / 1.0 |
| p10 / p05 | 0.99978 / 0.99897 |
| min | 0.93880 |
| CVaR₀.₁ | 0.99622 |
| **M6 = CVaR₀.₁ / mean (A2)** | **0.9966**, model-level bootstrap CI95 **[0.9942, 0.9983]** |
| pairs at AUC exactly 1.0 | **1169 / 1326 (88.2%)** |
| pairs with AUC < 0.99 | **12** |

**Primary test (A1), structure:** related pairs are lower-separability than unrelated
across the full distribution — Mann-Whitney **p = 3.5e-32**, dominance **0.689**,
model-level bootstrap **CI95 [0.606, 0.789]** (excludes 0.5). Mean separability
0.99714 (related) vs 0.99985 (unrelated).

**Secondary (descriptive, no longer carries the verdict):** near-relative enrichment
in the bottom decile **4.07×** (34.1% observed vs 8.4% base rate), permutation
**p = 0.0000** (10k draws).

So: the *ordering* is emphatically real and structured; the *spread* is not.
CVaR₀.₁ ≈ mean because 88% of the mass is pinned at the ceiling.

### R2 — Why the falsification is instrument-attributable, not population-level

Three independent reasons the saturation is a property of this measurement:

1. **The measurement is 2-way; the task is 52-way.** Our own full-test-set
   reproduction baseline is Top-1 ≈ 88–91% over 52 classes, so the real problem has
   large headroom. Telling A from B pairwise is far easier than picking A out of 52.
   **CVaR over *pairwise* separability is measuring the wrong object.**
2. **Templates are collapsed centroids — this violates I3.** Per P1 §F1
   (`templates.py:58`, `inference.py:188`), each template is a single mean vector;
   intra-model spread is discarded. D001 therefore measured trace-cloud vs a
   *noise-free point*, which inflates separability. I3 requires point-cloud vs
   point-cloud. The shipped artifacts cannot supply that.
3. **Resolution floor.** With 68×68 = 4624 comparisons per pair, the smallest
   resolvable sub-unity AUC is 0.99978. "AUC = 1.0" means *zero errors observed*,
   not *provably separable*. 88.2% of pairs sit on this floor, so the variable CVaR
   is computed over is censored for most of its mass.

Supporting evidence for P1 §F1, now measured rather than asserted: Pearson
correlation between **centroid distance and actual separability is only r = 0.242**.
Centroid geometry is a poor proxy for separability, exactly as P1 argued.

### R3 — M4, the motivating case (Review A3 limit applies)

`Meta-Llama-3-70B-Instruct` ↔ `abacusai/Smaug-Llama-3-70B-Instruct`:
AUC **0.9888**, confusion rate 0.044, **percentile rank 0.75** — the 11th hardest of
1326 pairs. **D001's pre-registered expectation is confirmed**: the paper's worst
case is an instance of a general pattern, not an isolated one.

**Per Review A3 this is NOT a reproduction of the paper's 84%.** That number comes
from the closed-set softmax classifier (Fig. G.1); this is open-set
nearest-template. Different systems, different quantities.

### R4 — M5 / I1 and the A1 recommendation

**I1 is satisfied comfortably** in a ≤14B universe: **65 near-relative pairs among
37 models** (requirement: ≥3). Subset statistics track the full set closely
(M6 = 0.9963, dominance 0.680, p = 5.2e-17). Strict-<14B sensitivity (35 models,
Phi-3-medium excluded) is materially identical: M6 = 0.9962. The Review's call to
**include** Phi-3-medium is vindicated — it contributes a genuine hard pair
(4k↔128k, AUC 0.9853, 0.60th percentile).

Best ≤14B confusion clusters, hardest first:

| AUC | pair |
|---|---|
| 0.9388 | Falcon3-7B ↔ Falcon3-10B *(hardest pair in the entire matrix)* |
| 0.9689 | Llama-3.2-1B ↔ Llama-3.2-3B |
| 0.9842 | Qwen2-1.5B ↔ Qwen2.5-0.5B |
| 0.9853 | Phi-3-medium-4k ↔ Phi-3-medium-128k |
| 0.9875 | Phi-3-mini-4k ↔ Phi-3-mini-128k |
| 0.9931 | Llama3-ChatQA-1.5-8B ↔ openchat-3.6-8b *(both Llama-3-8B derivatives)* |
| 0.9950 | Mistral-7B-Instruct-v0.2 ↔ v0.3 |

**A1 recommendation, with one tension worth the human's attention.** The ≤14B
default is viable and satisfies I1 with room to spare. But several of the strongest
near-relative pairs need a **>14B partner**, and a strict ≤14B universe therefore
**excludes D001's own motivating case**:

| AUC | pair (excluded from ≤14B) |
|---|---|
| 0.9552 | aya-23-8B ↔ aya-23-35B |
| 0.9743 | gemma-2-9b ↔ gemma-2-27b |
| 0.9851 | Llama-3-8B ↔ Llama-3-70B |
| 0.9888 | Llama-3-70B ↔ Smaug-70B *(the motivating case)* |

Recommend: keep the ~25×≤14B core, and if D002's measured budget allows, add **one
70B-class pair** (Llama-3-70B + Smaug-70B) to retain the motivating case. Whether
that is affordable is exactly D002's question.

### R5 — Interpretation limits carried into the record

- Separability = AUC of Δ = d(trace,T_B) − d(trace,T_A) under the **open-set
  nearest-template** classifier (Review A3).
- Hardness is measured **under the paper's existing 8 queries**, not hardness in
  principle — that is T2.4 and needs the full pool.
- This is **LLMmap0.2**, which the README states is not a one-to-one conversion of
  the paper's implementation. These are *our* baseline numbers; not reconciled
  against published ones.
- The AUC metric here is **scoped to D001 and is not a decision on A3**, per the
  Review's closing note.
- **S2, template provenance — resolved.** Templates recomputed from the train split
  match the shipped `templates.json` to max abs deviation **6.58e-05** against a
  template L2 scale of ~58.9 (relative ~1e-6, float32 accumulation noise). The
  shipped templates were built on **train only**, so M1 is **not** optimistically
  biased — closing the Review's conditional concern.
- Evaluation used the test split only (3536 traces, 68/model, balanced).

### R6 — What this does and does not license (design-side decision)

It does **not** license "the CDQD premise is dead". The structural premise is
confirmed at p = 3.5e-32 and the motivating case is at the 0.75th percentile. What
failed is the *exploitable-tail* premise **as measured through a saturated,
I3-violating, 2-way instrument**.

Recommended before any METHOD revision: re-measure the tail on a small pilot with a
statistic that (a) is I3-compliant (point-cloud vs point-cloud, not vs a collapsed
centroid) and (b) does not saturate — e.g. the A3 default (CV AUC of a linear probe
on point-cloud embeddings), or a k-way/confusion-based quantity that reflects the
52-way task. If the ratio stays ≈1 under a non-saturating I3-compliant statistic,
*that* would be a genuine falsification and would escalate to INVARIANT.

Flagging per the standing rule rather than working around it: **D001 as specified
could not test premise 1 on a non-saturating statistic**, because the shipped
artifacts only expose collapsed centroids.

<!-- TACC writes below this line -->

---
---

## Review

> *Append-only. Neither side edits the other's sections; comments go here.*

<!-- append below -->

---

### Housekeeping note (design-side, 2026-09-01)

The P1 review below was originally posted as a standalone file
(`docs/D001-Review-P1-v2.md`) instead of inline here, which is a deviation
from `CLAUDE.md`'s file-ownership rule (`## Review` lives in `docs/D{id}.md`).
Consolidated here now; the standalone file has been deleted so there is one
canonical location instead of two.

---

## Review — D001 / P1

### For you (human)

**Verdict:** APPROVED WITH AMENDMENTS

**What changed**
- The pass/fail test I originally wrote for D001 was too weak to actually
  detect anything (too few known cases to test against) — replaced it with
  a stronger test that uses all the data instead of a small slice of it.
- Added one new number to report: how different our target (worst-case)
  is from the standard target (average-case). This is the single most
  important number in this experiment — it's the direct answer to "is
  there anything here worth optimizing for."
- Added a caveat: this experiment can rank how confusable the Llama-70B /
  Smaug pair is, but under a different scoring method than the paper
  used — so it can't be reported as "reproducing" the paper's 84% number,
  only as a related but distinct measurement.

**Needs your input:** None for D001 itself.

**Follow-ups opened**
- **GPU access appears broken** on the TACC partition used (CPU-only
  torch, no GPU visible to the scheduler). Harmless for this small
  experiment (~40 min either way) but **blocks the next phase**, which
  needs a much larger run. Proposed as a new D — see start prompt for the
  local session.
- Two minor judgment calls (a borderline model-size cutoff, how to count
  parameters for a couple of models) were resolved on the design side.
  No action needed; noted for your awareness.

**Next:** TACC executes the approved plan; results return as D001's `## R`.

---

### Technical log (for TACC / audit trail — skip unless "For you" told you to)

P1 correctly resolved all three items D001 left open, and did so by
inspecting the code rather than assuming: it found that the shipped
"template" for each model is a single averaged vector with all
per-response spread discarded, correctly concluded that centroid
distance therefore cannot serve as the separability measure, and
redesigned the metric around traces instead. That is exactly the
behavior the open items in D001 were meant to test for.

#### A1 — Amendment to the D-section criteria (design-side error, now corrected)

P1 §S4 is right that the decile-enrichment test has almost no power: with
~8–10 documented base↔fine-tune pairs among 52 models, the null
expectation inside a 10% decile is ≈1. That criterion was poorly
specified in D001. Rather than have P work around it, the criterion
itself is amended.

**The `What counts as an answer` table in D001 is replaced by:**

| Outcome | Criterion |
|---|---|
| **CONFIRMS** | *(primary)* Structurally-related pairs have significantly lower separability than unrelated pairs across the **full** distribution (Mann-Whitney over all 1326 pairs, model-level bootstrap CI excluding no-effect), **and** the CVaR/mean ratio (see A2) is meaningfully below 1. |
| **FALSIFIES** | The related-vs-unrelated distributions are statistically indistinguishable, **or** the CVaR/mean ratio ≈ 1 (no exploitable tail). |
| **INCONCLUSIVE** | CIs span no-effect on the primary test. Report; do not force a verdict. |

Decile enrichment is retained as a **descriptive secondary** statistic —
report it, but it no longer carries the verdict. The permutation test and
model-level bootstrap proposed in P1 §S4 are both accepted as specified.

#### A2 — New measurement M6 (add to S3; also compute for the ≤14B subset in S6)

**M6 — Objective divergence ratio.**

```
CVaR_0.1(separability)  /  mean(separability)
```

over all pairs, and separately over the ≤14B subset. This is the direct
numerical answer to "does a CVaR objective differ from a mean objective
on this population" — a ratio near 1 means the two objectives coincide
and the premise fails (the silent-null failure mode invariant I1 exists
to catch). One line of code; report prominently in R alongside the
percentiles.

#### A3 — Interpretation limit to carry into R

The AUC of `Δ = d(trace,T_B) − d(trace,T_A)` measures separability under
the **open-set nearest-template** classifier. The paper's 84% for
Meta-Llama-3-70B-Instruct comes from the **closed-set softmax**
classifier (Figure G.1). Different systems. M4 may report the pair's
percentile rank *under our open-set measure*; it must **not** be reported
as reproducing the paper's 84% confusion. State this explicitly in R.

#### Judgment calls from P1 §S6 — resolved

- **`Phi-3-medium-{4k,128k}` at exactly 14B → INCLUDE.** The boundary is
  soft, and the 4k↔128k pair is a valuable near-relative candidate.
  Excluding it discards ammunition for the very structure D001 tests for.
  Report the 37/9/6 split as primary, 35/11/6 as sensitivity.
- **MoE under total-parameter accounting → AGREED.** Memory residency is
  set by total parameters, not active ones, so total is the correct basis
  for feasibility.

#### Everything else in P1 — accepted as written

S2 (template provenance recomputation): accepted — cheap, resolves
whether shipped templates were built on train only. If not, M1 is
optimistically biased and R must say so.

Test-split-only evaluation (3536 traces, 68/model) is correct and should
be stated as such in R — evaluating with train traces against
train-built templates would be optimistically biased.

The label-order guard in S1 is exactly right. Keep the "stop, do not
patch around it" response if it trips.

#### Design-side follow-ups (not blockers for D001)

1. **GPU availability must be resolved before the corpus-building phase.**
   P1 §F5 reports `torch 2.7.1+cpu` and no GRES advertised on `gh`/`gh-dev`.
   Harmless for D001 (~40 min CPU). The next phase is ~10⁵ generations and
   is not feasible on CPU at that scale. Needs a dedicated D to establish
   GPU access under Slurm and a measured throughput number — not an
   estimate — before that phase's cost can be planned.
2. Two aarch64 fixes were made to the repo (`torch.load()` device
   handling; missing `accelerate` dependency) **before** D001 was handed
   over, i.e. outside any REVIEW-gated plan — confirmed with the human,
   no process violation. Logged here for the audit trail regardless,
   since one of the two touches checkpoint loading and is therefore
   research-relevant, not purely cosmetic.
3. `CLAUDE.md`, `METHOD.md`, and `DECISIONS.md` are now being pushed to
   this repo, resolving P1 §6's open item. Future D's involving the model
   universe (I1) can reference these directly instead of reporting
   descriptively.

#### Note for A1 (model universe) — do not let M1 become A3

The AUC metric adopted here is scoped to D001 only. It is **not** a
decision on the separability statistic for the real tensor (open decision
A3 in `DECISIONS.md`). D001 measures traces against a collapsed centroid;
the tensor measures point cloud against point cloud in embedding space —
different quantities. A3 remains open.

---

## Review — D001 results (post-hoc summary, no second approval needed)

### For you (human)

**Verdict:** N/A — this is a summary of `## R`, not an approval gate. D001
already executed under the P1 approval above.

**What the results say, in one paragraph:** The two premises D001 tested
came apart. "Hard pairs are related models" is confirmed decisively (11 of
the 12 hardest pairs are near-relatives). "A heavy hard tail exists" came
back negative — but that negative result is itself suspect: 88% of all
pairs were measured as perfectly separable, which points at the
measuring stick (a two-way comparison against a flattened average, with
limited resolution) rather than at reality. Full plain-language breakdown
posted separately in this session — ask if you want it repeated here.

**Needs your input:**
- A1 (model universe): D001 recommends the ~25×≤14B core plus one 70B-class
  pair (Llama-3-70B + Smaug-70B) if D002's budget allows it. This is an A1
  decision per `DECISIONS.md`, still open.

**Follow-ups opened:**
- **[D004](D004.md)** — re-measures the hard-tail premise with a statistic
  that is I3-compliant and does not saturate, per R6. Approved by the human
  2026-09-01; drafted and awaiting a `## P` from TACC.

**Next:** D001 stays open until D004 reports. A1 remains open independently.

---

### Technical log

See `## R` in this file for the full numbers (R1–R6); `results/D001/` for
raw data. Cross-checked `results/D001/summary.json` against the prose in
`## R` — all headline figures (M6=0.9966, dominance=0.689,
p=3.5e-32, percentile ranks, ≤14B subset counts) match the underlying data
file exactly. No independent re-derivation of the statistics was performed
beyond this consistency check; that was judged sufficient given TACC's R
already documents its own instrument-attribution reasoning (R2) in detail.

D001's own status/closed fields in this file's header block are being left
as `status: OPEN` / `closed: —` deliberately — R6's open question means this
D is not yet closed out, pending the human's R6 decision above.

---

### TACC note appended 2026-09-02 — two silent I2 threats found in the generator

> Appended per `CLAUDE.md`'s file-ownership rule ("comments on someone else's
> section go in `## Review` as an appended note"). Reporting only — **no code was
> changed.** Both items touch invariant **I2**, which is a human-escalation
> category, and neither falls inside any D currently assigned to TACC (D002 is
> infrastructure; D004 is re-analysis). Raised here because it bears on how D001's
> own traces should be interpreted and on what Phase 1 will silently do.

**(1) `sample()` drops its `pool` argument — the prompt-config holdout is never applied.**

```python
# LLMmap/prompt_configuration.py:234-240
def sample(self, n, pool=TRAIN):
    s = set()
    while len(s) != n:
        s.add(self.sample_one())   # <-- pool never forwarded
    return list(s)
```

`dataset_maker.py:60-61` calls `sample(..., pool=TRAIN)` and `sample(..., pool=TEST)`,
so **both splits draw system / CoT / RAG prompts from the TRAIN pool.**

Everything around it is correct, which is what makes this easy to miss:
`confs/prompt_configurations/train_test_split.json` defines a genuinely disjoint
split (systems 51/33, cot 4/2, rag 6/3 — **index overlap 0**), and `_cond_choice`
honours `pool` properly. The defect is one missing argument on line 239.

**(2) Sampling hyper-parameters have no train/test split at all.**
`sample_one` draws them via `sample_from_multi_universe(self.sampling_universe)`
(line 211), and `sampling_universe` in `confs/prompt_configurations/general.json` is
a single flat set of `do_sample` / `temperature` values with no pool structure. The
paper (§7.1) splits `H` into `Htrain`/`Htest`; **the released code does not implement
that split.** So even with (1) fixed, sampling settings still cross splits.

**Why this matters — I2 exactly.** I2 exists to prevent selection-on-test. CDQD's
central operation is *selecting queries against a val/test split*; with config
leakage, selected queries look like they generalize while actually being tuned to
conditions the model saw in training. The code runs and produces numbers with **no
error** — the invariants' stated failure mode.

**Bearing on D001/D004.** Whether the shipped `default_dataset.jsonl` carries this
defect **cannot be determined from the artifact**: its entries have no `prompt_conf`
field, so it predates the current code path. If it does, D001's "test split" is not
a clean holdout, which would inflate separability and is a **fourth** candidate
explanation for the saturation, alongside D001 R2's three. Recorded in D004/P1 §5.

**Not fixed deliberately.** Item (1) is a one-line change (`self.sample_one(pool)`);
item (2) needs a config schema change plus code. Both are method-affecting and touch
an invariant, so per the code-change rule they warrant their own D or an explicit
design-side decision rather than a drive-by fix from TACC.
